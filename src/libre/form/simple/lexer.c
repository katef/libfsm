/* Generated by lx */

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <errno.h>

#include <unistd.h>
#include <fcntl.h>

#include LX_HEADER

static enum lx_token z1(struct lx *lx);
static enum lx_token z2(struct lx *lx);
static enum lx_token z3(struct lx *lx);

static int
lx_getc(struct lx *lx)
{
	int c;

	assert(lx != NULL);
	assert(lx->lgetc != NULL);

	if (lx->c != EOF) {
		c = lx->c, lx->c = EOF;
	} else {
		c = lx->lgetc(lx);
		if (c == EOF) {
			return EOF;
		}
	}

	lx->end.byte++;
	lx->end.col++;

	if (c == '\n') {
		lx->end.line++;
		lx->end.col = 1;
	}

	return c;
}

static void
lx_simple_ungetc(struct lx *lx, int c)
{
	assert(lx != NULL);
	assert(lx->c == EOF);

	lx->c = c;

	if (lx->pop != NULL) {
		lx->pop(lx);
	}

	lx->end.byte--;
	lx->end.col--;

	if (c == '\n') {
		lx->end.line--;
		lx->end.col = 0; /* XXX: lost information */
	}
}

int
lx_simple_fgetc(struct lx *lx)
{
	assert(lx != NULL);
	assert(lx->opaque != NULL);

	return fgetc(lx->opaque);
}

int
lx_simple_sgetc(struct lx *lx)
{
	char *s;

	assert(lx != NULL);
	assert(lx->opaque != NULL);

	s = lx->opaque;
	if (*s == '\0') {
		return EOF;
	}

	return lx->opaque = s + 1, *s;
}

int
lx_simple_agetc(struct lx *lx)
{
	struct lx_arr *a;

	assert(lx != NULL);
	assert(lx->opaque != NULL);

	a = lx->opaque;

	assert(a != NULL);
	assert(a->p != NULL);

	if (a->len == 0) {
		return EOF;
	}

	return a->len--, *a->p++;
}

int
lx_simple_dgetc(struct lx *lx)
{
	struct lx_fd *d;

	assert(lx != NULL);
	assert(lx->opaque != NULL);

	d = lx->opaque;
	assert(d->fd != -1);
	assert(d->p != NULL);

	if (d->len == 0) {
		ssize_t r;

		assert((fcntl(d->fd, F_GETFL) & O_NONBLOCK) == 0);

		d->p = (char *) d + sizeof *d;

		r = read(d->fd, d->p, d->bufsz);
		if (r == -1) {
			assert(errno != EAGAIN);
			return EOF;
		}

		if (r == 0) {
			return EOF;
		}

		d->len = r;
	}

	return d->len--, *d->p++;
}

int
lx_simple_dynpush(struct lx *lx, char c)
{
	struct lx_dynbuf *t;

	assert(lx != NULL);
	assert(c != EOF);

	t = lx->buf;

	assert(t != NULL);

	if (t->p == t->a + t->len) {
		size_t len;
		char *tmp;

		if (t->len == 0) {
			assert(LX_DYN_LOW > 0);
			len = LX_DYN_LOW;
		} else {
			len = t->len * LX_DYN_FACTOR;
			if (len < t->len) {
				errno = ERANGE;
				return -1;
			}
		}

		tmp = realloc(t->a, len);
		if (tmp == NULL) {
			return -1;
		}

		t->p   = tmp + (t->p - t->a);
		t->a   = tmp;
		t->len = len;
	}

	assert(t->p != NULL);
	assert(t->a != NULL);

	*t->p++ = c;

	return 0;
}

void
lx_simple_dynpop(struct lx *lx)
{
	struct lx_dynbuf *t;

	assert(lx != NULL);

	t = lx->buf;

	assert(t != NULL);
	assert(t->a != NULL);
	assert(t->p >= t->a);

	if (t->p == t->a) {
		return;
	}

	t->p--;
}

int
lx_simple_dynclear(struct lx *lx)
{
	struct lx_dynbuf *t;

	assert(lx != NULL);

	t = lx->buf;

	assert(t != NULL);

	if (t->len > LX_DYN_HIGH) {
		size_t len;
		char *tmp;

		len = t->len / LX_DYN_FACTOR;

		tmp = realloc(t->a, len);
		if (tmp == NULL) {
			return -1;
		}

		t->a   = tmp;
		t->len = len;
	}

	t->p = t->a;

	return 0;
}

void
lx_simple_dynfree(struct lx *lx)
{
	struct lx_dynbuf *t;

	assert(lx != NULL);

	t = lx->buf;

	assert(t != NULL);

	free(t->a);
}
int
lx_simple_fixedpush(struct lx *lx, char c)
{
	struct lx_fixedbuf *t;

	assert(lx != NULL);
	assert(c != EOF);

	t = lx->buf;

	assert(t != NULL);
	assert(t->p != NULL);
	assert(t->a != NULL);

	if (t->p == t->a + t->len) {
		errno = ENOMEM;
		return -1;
	}

	*t->p++ = c;

	return 0;
}

void
lx_simple_fixedpop(struct lx *lx)
{
	struct lx_fixedbuf *t;

	assert(lx != NULL);

	t = lx->buf;

	assert(t != NULL);
	assert(t->a != NULL);
	assert(t->p >= t->a);

	if (t->p == t->a) {
		return;
	}

	t->p--;
}

int
lx_simple_fixedclear(struct lx *lx)
{
	struct lx_fixedbuf *t;

	assert(lx != NULL);

	t = lx->buf;

	assert(t != NULL);
	assert(t->p != NULL);
	assert(t->a != NULL);

	t->p = t->a;

	return 0;
}

static enum lx_token
z1(struct lx *lx)
{
	int c;

	enum {
		S1, S2, S3, S4
	} state;

	assert(lx != NULL);

	if (lx->clear != NULL) {
		lx->clear(lx);
	}

	state = S4;

	lx->start = lx->end;

	while (c = lx_getc(lx), c != EOF) {
		switch (state) {
			break;

		default:
			if (lx->push != NULL) {
				if (-1 == lx->push(lx, c)) {
					return TOK_ERROR;
				}
			}
			break;

		}

		switch (state) {
		case S1:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_SEP;
			}

		case S2:
			switch (c) {
			case '0':	          continue;
			case '1':	          continue;
			case '2':	          continue;
			case '3':	          continue;
			case '4':	          continue;
			case '5':	          continue;
			case '6':	          continue;
			case '7':	          continue;
			case '8':	          continue;
			case '9':	          continue;
			default:  lx_simple_ungetc(lx, c); return TOK_COUNT;
			}

		case S3:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return lx->z = z3, TOK_CLOSECOUNT;
			}

		case S4:
			switch (c) {
			case ',': state = S1;      continue;
			case '0': state = S2;      continue;
			case '1': state = S2;      continue;
			case '2': state = S2;      continue;
			case '3': state = S2;      continue;
			case '4': state = S2;      continue;
			case '5': state = S2;      continue;
			case '6': state = S2;      continue;
			case '7': state = S2;      continue;
			case '8': state = S2;      continue;
			case '9': state = S2;      continue;
			case '}': state = S3;      continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}
		}
	}

	lx->lgetc = NULL;

	switch (state) {
	case S1: return TOK_SEP;
	case S2: return TOK_COUNT;
	case S3: return TOK_CLOSECOUNT;
	default: errno = EINVAL; return TOK_ERROR;
	}
}

static enum lx_token
z2(struct lx *lx)
{
	int c;

	enum {
		S1, S2, S3, S4, S5, S6, S7
	} state;

	assert(lx != NULL);

	if (lx->clear != NULL) {
		lx->clear(lx);
	}

	state = S7;

	lx->start = lx->end;

	while (c = lx_getc(lx), c != EOF) {
		switch (state) {
			break;

		default:
			if (lx->push != NULL) {
				if (-1 == lx->push(lx, c)) {
					return TOK_ERROR;
				}
			}
			break;

		}

		switch (state) {
		case S1:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_ESC;
			}

		case S2:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_RANGE;
			}

		case S3:
			switch (c) {
			case '\\': state = S1;      continue;
			case 'f': state = S1;      continue;
			case 'n': state = S1;      continue;
			case 'r': state = S1;      continue;
			case 't': state = S1;      continue;
			case 'v': state = S1;      continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S4:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return lx->z = z3, TOK_CLOSEGROUP;
			}

		case S5:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_INVERT;
			}

		case S6:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_CHAR;
			}

		case S7:
			switch (c) {
			case '-': state = S2;      continue;
			case '0': state = S6;      continue;
			case '1': state = S6;      continue;
			case '2': state = S6;      continue;
			case '3': state = S6;      continue;
			case '4': state = S6;      continue;
			case '5': state = S6;      continue;
			case '6': state = S6;      continue;
			case '7': state = S6;      continue;
			case '8': state = S6;      continue;
			case '9': state = S6;      continue;
			case 'A': state = S6;      continue;
			case 'B': state = S6;      continue;
			case 'C': state = S6;      continue;
			case 'D': state = S6;      continue;
			case 'E': state = S6;      continue;
			case 'F': state = S6;      continue;
			case 'G': state = S6;      continue;
			case 'H': state = S6;      continue;
			case 'I': state = S6;      continue;
			case 'J': state = S6;      continue;
			case 'K': state = S6;      continue;
			case 'L': state = S6;      continue;
			case 'M': state = S6;      continue;
			case 'N': state = S6;      continue;
			case 'O': state = S6;      continue;
			case 'P': state = S6;      continue;
			case 'Q': state = S6;      continue;
			case 'R': state = S6;      continue;
			case 'S': state = S6;      continue;
			case 'T': state = S6;      continue;
			case 'U': state = S6;      continue;
			case 'V': state = S6;      continue;
			case 'W': state = S6;      continue;
			case 'X': state = S6;      continue;
			case 'Y': state = S6;      continue;
			case 'Z': state = S6;      continue;
			case '\\': state = S3;      continue;
			case ']': state = S4;      continue;
			case '^': state = S5;      continue;
			case '_': state = S6;      continue;
			case 'a': state = S6;      continue;
			case 'b': state = S6;      continue;
			case 'c': state = S6;      continue;
			case 'd': state = S6;      continue;
			case 'e': state = S6;      continue;
			case 'f': state = S6;      continue;
			case 'g': state = S6;      continue;
			case 'h': state = S6;      continue;
			case 'i': state = S6;      continue;
			case 'j': state = S6;      continue;
			case 'k': state = S6;      continue;
			case 'l': state = S6;      continue;
			case 'm': state = S6;      continue;
			case 'n': state = S6;      continue;
			case 'o': state = S6;      continue;
			case 'p': state = S6;      continue;
			case 'q': state = S6;      continue;
			case 'r': state = S6;      continue;
			case 's': state = S6;      continue;
			case 't': state = S6;      continue;
			case 'u': state = S6;      continue;
			case 'v': state = S6;      continue;
			case 'w': state = S6;      continue;
			case 'x': state = S6;      continue;
			case 'y': state = S6;      continue;
			case 'z': state = S6;      continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}
		}
	}

	lx->lgetc = NULL;

	switch (state) {
	case S1: return TOK_ESC;
	case S2: return TOK_RANGE;
	case S4: return TOK_CLOSEGROUP;
	case S5: return TOK_INVERT;
	case S6: return TOK_CHAR;
	default: errno = EINVAL; return TOK_ERROR;
	}
}

static enum lx_token
z3(struct lx *lx)
{
	int c;

	enum {
		S1, S2, S3, S4, S5, S6, S7, S8, S9, S10, 
		S11, S12, S13, S14, S15
	} state;

	assert(lx != NULL);

	if (lx->clear != NULL) {
		lx->clear(lx);
	}

	state = S15;

	lx->start = lx->end;

	while (c = lx_getc(lx), c != EOF) {
		switch (state) {
			break;

		default:
			if (lx->push != NULL) {
				if (-1 == lx->push(lx, c)) {
					return TOK_ERROR;
				}
			}
			break;

		}

		switch (state) {
		case S1:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_ESC;
			}

		case S2:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_EOL;
			}

		case S3:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_OPENSUB;
			}

		case S4:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_CLOSESUB;
			}

		case S5:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_STAR;
			}

		case S6:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_PLUS;
			}

		case S7:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_DOT;
			}

		case S8:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_QMARK;
			}

		case S9:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return lx->z = z2, TOK_OPENGROUP;
			}

		case S10:
			switch (c) {
			case '\\': state = S1;      continue;
			case 'f': state = S1;      continue;
			case 'n': state = S1;      continue;
			case 'r': state = S1;      continue;
			case 't': state = S1;      continue;
			case 'v': state = S1;      continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S11:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_SOL;
			}

		case S12:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_CHAR;
			}

		case S13:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return lx->z = z1, TOK_OPENCOUNT;
			}

		case S14:
			switch (c) {
			default:  lx_simple_ungetc(lx, c); return TOK_ALT;
			}

		case S15:
			switch (c) {
			case '$': state = S2;      continue;
			case '(': state = S3;      continue;
			case ')': state = S4;      continue;
			case '*': state = S5;      continue;
			case '+': state = S6;      continue;
			case '.': state = S7;      continue;
			case '0': state = S12;      continue;
			case '1': state = S12;      continue;
			case '2': state = S12;      continue;
			case '3': state = S12;      continue;
			case '4': state = S12;      continue;
			case '5': state = S12;      continue;
			case '6': state = S12;      continue;
			case '7': state = S12;      continue;
			case '8': state = S12;      continue;
			case '9': state = S12;      continue;
			case '?': state = S8;      continue;
			case 'A': state = S12;      continue;
			case 'B': state = S12;      continue;
			case 'C': state = S12;      continue;
			case 'D': state = S12;      continue;
			case 'E': state = S12;      continue;
			case 'F': state = S12;      continue;
			case 'G': state = S12;      continue;
			case 'H': state = S12;      continue;
			case 'I': state = S12;      continue;
			case 'J': state = S12;      continue;
			case 'K': state = S12;      continue;
			case 'L': state = S12;      continue;
			case 'M': state = S12;      continue;
			case 'N': state = S12;      continue;
			case 'O': state = S12;      continue;
			case 'P': state = S12;      continue;
			case 'Q': state = S12;      continue;
			case 'R': state = S12;      continue;
			case 'S': state = S12;      continue;
			case 'T': state = S12;      continue;
			case 'U': state = S12;      continue;
			case 'V': state = S12;      continue;
			case 'W': state = S12;      continue;
			case 'X': state = S12;      continue;
			case 'Y': state = S12;      continue;
			case 'Z': state = S12;      continue;
			case '[': state = S9;      continue;
			case '\\': state = S10;      continue;
			case '^': state = S11;      continue;
			case '_': state = S12;      continue;
			case 'a': state = S12;      continue;
			case 'b': state = S12;      continue;
			case 'c': state = S12;      continue;
			case 'd': state = S12;      continue;
			case 'e': state = S12;      continue;
			case 'f': state = S12;      continue;
			case 'g': state = S12;      continue;
			case 'h': state = S12;      continue;
			case 'i': state = S12;      continue;
			case 'j': state = S12;      continue;
			case 'k': state = S12;      continue;
			case 'l': state = S12;      continue;
			case 'm': state = S12;      continue;
			case 'n': state = S12;      continue;
			case 'o': state = S12;      continue;
			case 'p': state = S12;      continue;
			case 'q': state = S12;      continue;
			case 'r': state = S12;      continue;
			case 's': state = S12;      continue;
			case 't': state = S12;      continue;
			case 'u': state = S12;      continue;
			case 'v': state = S12;      continue;
			case 'w': state = S12;      continue;
			case 'x': state = S12;      continue;
			case 'y': state = S12;      continue;
			case 'z': state = S12;      continue;
			case '{': state = S13;      continue;
			case '|': state = S14;      continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}
		}
	}

	lx->lgetc = NULL;

	switch (state) {
	case S1: return TOK_ESC;
	case S2: return TOK_EOL;
	case S3: return TOK_OPENSUB;
	case S4: return TOK_CLOSESUB;
	case S5: return TOK_STAR;
	case S6: return TOK_PLUS;
	case S7: return TOK_DOT;
	case S8: return TOK_QMARK;
	case S9: return TOK_OPENGROUP;
	case S11: return TOK_SOL;
	case S12: return TOK_CHAR;
	case S13: return TOK_OPENCOUNT;
	case S14: return TOK_ALT;
	default: errno = EINVAL; return TOK_ERROR;
	}
}

const char *
lx_simple_name(enum lx_token t)
{
	switch (t) {
	case TOK_CLOSECOUNT: return "CLOSECOUNT";
	case TOK_OPENCOUNT: return "OPENCOUNT";
	case TOK_COUNT: return "COUNT";
	case TOK_SEP: return "SEP";
	case TOK_CLOSEGROUP: return "CLOSEGROUP";
	case TOK_OPENGROUP: return "OPENGROUP";
	case TOK_RANGE: return "RANGE";
	case TOK_INVERT: return "INVERT";
	case TOK_CHAR: return "CHAR";
	case TOK_ESC: return "ESC";
	case TOK_CLOSESUB: return "CLOSESUB";
	case TOK_OPENSUB: return "OPENSUB";
	case TOK_ALT: return "ALT";
	case TOK_DOT: return "DOT";
	case TOK_PLUS: return "PLUS";
	case TOK_STAR: return "STAR";
	case TOK_QMARK: return "QMARK";
	case TOK_EOL: return "EOL";
	case TOK_SOL: return "SOL";
	case TOK_EOF:     return "EOF";
	case TOK_ERROR:   return "ERROR";
	case TOK_UNKNOWN: return "UNKNOWN";
	default: return "?";
	}
}

void
lx_simple_init(struct lx *lx)
{
	static const struct lx lx_default;

	assert(lx != NULL);

	*lx = lx_default;

	lx->c = EOF;
	lx->z = NULL;

	lx->end.byte = 0;
	lx->end.line = 1;
	lx->end.col  = 1;
}

enum lx_token
lx_simple_next(struct lx *lx)
{
	enum lx_token t;

	assert(lx != NULL);

	if (lx->lgetc == NULL) {
		return TOK_EOF;
	}

	if (lx->z == NULL) {
		lx->z = z3;
	}

	t = lx->z(lx);

	if (lx->push != NULL) {
		if (-1 == lx->push(lx, '\0')) {
			return TOK_ERROR;
		}
	}

	if (lx->lgetc == NULL && lx->free != NULL) {
		lx->free(lx);
	}

	return t;
}

