/* Generated by libfsm */

#include LF_HEADER

#include <stddef.h>

#include <fsm/fsm.h>

int
utf8_Cs_fsm(struct fsm *fsm, fsm_state_t x, fsm_state_t y)
{
	fsm_state_t s[1];
	size_t i;

	(void)y; /* unused */

	for (i = 0; i < 1; i++) {
		if (i == 0) {
			s[0] = x;
			continue;
		}

		if (!fsm_addstate(fsm, &s[i])) {
			return 0;
		}
	}

	return 1;
}

