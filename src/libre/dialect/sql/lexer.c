/* Generated by lx */

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <errno.h>

#include LX_HEADER

static enum lx_sql_token z0(struct lx_sql_lx *lx);
static enum lx_sql_token z1(struct lx_sql_lx *lx);

#if __STDC_VERSION__ >= 199901L
inline
#endif
static int
lx_getc(struct lx_sql_lx *lx)
{
	int c;

	assert(lx != NULL);
	assert(lx->lgetc != NULL);

	if (lx->c != EOF) {
		c = lx->c, lx->c = EOF;
	} else {
		c = lx->lgetc(lx);
		if (c == EOF) {
			return EOF;
		}
	}

	lx->end.byte++;
	lx->end.col++;

	if (c == '\n') {
		lx->end.line++;
		lx->end.col = 1;
	}

	return c;
}

#if __STDC_VERSION__ >= 199901L
inline
#endif
static void
lx_sql_ungetc(struct lx_sql_lx *lx, int c)
{
	assert(lx != NULL);
	assert(lx->c == EOF);

	lx->c = c;

	if (lx->pop != NULL) {
		lx->pop(lx);
	}

	lx->end.byte--;
	lx->end.col--;

	if (c == '\n') {
		lx->end.line--;
		lx->end.col = 0; /* XXX: lost information */
	}
}

int
lx_sql_dynpush(struct lx_sql_lx *lx, char c)
{
	struct lx_dynbuf *t;

	assert(lx != NULL);
	assert(c != EOF);

	t = lx->buf;

	assert(t != NULL);

	if (t->p == t->a + t->len) {
		size_t len;
		char *tmp;

		if (t->len == 0) {
			assert(LX_DYN_LOW > 0);
			len = LX_DYN_LOW;
		} else {
			len = t->len * LX_DYN_FACTOR;
			if (len < t->len) {
				errno = ERANGE;
				return -1;
			}
		}

		tmp = realloc(t->a, len);
		if (tmp == NULL) {
			return -1;
		}

		t->p   = tmp + (t->p - t->a);
		t->a   = tmp;
		t->len = len;
	}

	assert(t->p != NULL);
	assert(t->a != NULL);

	*t->p++ = c;

	return 0;
}

void
lx_sql_dynpop(struct lx_sql_lx *lx)
{
	struct lx_dynbuf *t;

	assert(lx != NULL);

	t = lx->buf;

	assert(t != NULL);
	assert(t->a != NULL);
	assert(t->p >= t->a);

	if (t->p == t->a) {
		return;
	}

	t->p--;
}

int
lx_sql_dynclear(struct lx_sql_lx *lx)
{
	struct lx_dynbuf *t;

	assert(lx != NULL);

	t = lx->buf;

	assert(t != NULL);

	if (t->len > LX_DYN_HIGH) {
		size_t len;
		char *tmp;

		len = t->len / LX_DYN_FACTOR;

		tmp = realloc(t->a, len);
		if (tmp == NULL) {
			return -1;
		}

		t->a   = tmp;
		t->len = len;
	}

	t->p = t->a;

	return 0;
}

void
lx_sql_dynfree(struct lx_sql_lx *lx)
{
	struct lx_dynbuf *t;

	assert(lx != NULL);

	t = lx->buf;

	assert(t != NULL);

	free(t->a);
}
static enum lx_sql_token
z0(struct lx_sql_lx *lx)
{
	int c;

	enum {
		S0, S1, S2, S3, S4, S5, S6, S7, S8, S9, 
		S10, S11, S12, S13, S14, S15, S16, S17, S18, S19, 
		S20, S21, S22, S23, S24, S25, S26, S27, S28, S29, 
		S30, S31, S32, S33, S34, S35, S36, S37, S38, S39, 
		S40, S41, S42, S43, S44, S45, S46, S47, S48, S49, 
		S50, S51, S52, S53, S54, S55, S56, S57, S58
	} state;

	assert(lx != NULL);

	if (lx->clear != NULL) {
		lx->clear(lx);
	}

	state = S0;

	lx->start = lx->end;

	while (c = lx_getc(lx), c != EOF) {
		if (lx->push != NULL) {
			if (-1 == lx->push(lx, c)) {
				return TOK_ERROR;
			}
		}

		switch (state) {
		case S0: /* start */
			switch (c) {
			case '\x00':
			case '\x01':
			case '\x02':
			case '\x03':
			case '\x04':
			case '\x05':
			case '\x06':
			case '\a':
			case '\b':
			case '\t':
			case '\n':
			case '\v':
			case '\f':
			case '\r':
			case '\x0e':
			case '\x0f':
			case '\x10':
			case '\x11':
			case '\x12':
			case '\x13':
			case '\x14':
			case '\x15':
			case '\x16':
			case '\x17':
			case '\x18':
			case '\x19':
			case '\x1a':
			case '\x1b':
			case '\x1c':
			case '\x1d':
			case '\x1e':
			case '\x1f':
			case ' ':
			case '!':
			case '"':
			case '#':
			case '$':
			case '%':
			case '&':
			case '\'':
			case '(':
			case ')':
			case '*':
			case '+':
			case ',': state = S1; continue;
			case '-': state = S2; continue;
			case '.':
			case '\x2f':
			case '0':
			case '1':
			case '2':
			case '3':
			case '4':
			case '5':
			case '6':
			case '7':
			case '8':
			case '9':
			case ':':
			case ';':
			case '<':
			case '=':
			case '>':
			case '?':
			case '@':
			case 'A':
			case 'B':
			case 'C':
			case 'D':
			case 'E':
			case 'F':
			case 'G':
			case 'H':
			case 'I':
			case 'J':
			case 'K':
			case 'L':
			case 'M':
			case 'N':
			case 'O':
			case 'P':
			case 'Q':
			case 'R':
			case 'S':
			case 'T':
			case 'U':
			case 'V':
			case 'W':
			case 'X':
			case 'Y':
			case 'Z': state = S1; continue;
			case '[': state = S3; continue;
			case '\\': state = S1; continue;
			case ']': state = S4; continue;
			case '^': state = S5; continue;
			case '_':
			case '`':
			case 'a':
			case 'b':
			case 'c':
			case 'd':
			case 'e':
			case 'f':
			case 'g':
			case 'h':
			case 'i':
			case 'j':
			case 'k':
			case 'l':
			case 'm':
			case 'n':
			case 'o':
			case 'p':
			case 'q':
			case 'r':
			case 's':
			case 't':
			case 'u':
			case 'v':
			case 'w':
			case 'x':
			case 'y':
			case 'z':
			case '{':
			case '|':
			case '}':
			case '~':
			case '\x7f':
			case '\x80':
			case '\x81':
			case '\x82':
			case '\x83':
			case '\x84':
			case '\x85':
			case '\x86':
			case '\x87':
			case '\x88':
			case '\x89':
			case '\x8a':
			case '\x8b':
			case '\x8c':
			case '\x8d':
			case '\x8e':
			case '\x8f':
			case '\x90':
			case '\x91':
			case '\x92':
			case '\x93':
			case '\x94':
			case '\x95':
			case '\x96':
			case '\x97':
			case '\x98':
			case '\x99':
			case '\x9a':
			case '\x9b':
			case '\x9c':
			case '\x9d':
			case '\x9e':
			case '\x9f':
			case '\xa0':
			case '\xa1':
			case '\xa2':
			case '\xa3':
			case '\xa4':
			case '\xa5':
			case '\xa6':
			case '\xa7':
			case '\xa8':
			case '\xa9':
			case '\xaa':
			case '\xab':
			case '\xac':
			case '\xad':
			case '\xae':
			case '\xaf':
			case '\xb0':
			case '\xb1':
			case '\xb2':
			case '\xb3':
			case '\xb4':
			case '\xb5':
			case '\xb6':
			case '\xb7':
			case '\xb8':
			case '\xb9':
			case '\xba':
			case '\xbb':
			case '\xbc':
			case '\xbd':
			case '\xbe':
			case '\xbf':
			case '\xc0':
			case '\xc1':
			case '\xc2':
			case '\xc3':
			case '\xc4':
			case '\xc5':
			case '\xc6':
			case '\xc7':
			case '\xc8':
			case '\xc9':
			case '\xca':
			case '\xcb':
			case '\xcc':
			case '\xcd':
			case '\xce':
			case '\xcf':
			case '\xd0':
			case '\xd1':
			case '\xd2':
			case '\xd3':
			case '\xd4':
			case '\xd5':
			case '\xd6':
			case '\xd7':
			case '\xd8':
			case '\xd9':
			case '\xda':
			case '\xdb':
			case '\xdc':
			case '\xdd':
			case '\xde':
			case '\xdf':
			case '\xe0':
			case '\xe1':
			case '\xe2':
			case '\xe3':
			case '\xe4':
			case '\xe5':
			case '\xe6':
			case '\xe7':
			case '\xe8':
			case '\xe9':
			case '\xea':
			case '\xeb':
			case '\xec':
			case '\xed':
			case '\xee':
			case '\xef':
			case '\xf0':
			case '\xf1':
			case '\xf2':
			case '\xf3':
			case '\xf4':
			case '\xf5':
			case '\xf6':
			case '\xf7':
			case '\xf8':
			case '\xf9':
			case '\xfa':
			case '\xfb':
			case '\xfc':
			case '\xfd':
			case '\xfe':
			case '\xff': state = S1; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S1: /* e.g. "a" */
			lx_sql_ungetc(lx, c); return TOK_CHAR;

		case S2: /* e.g. "-" */
			lx_sql_ungetc(lx, c); return TOK_RANGE;

		case S3: /* e.g. "[" */
			switch (c) {
			case ':': state = S6; continue;
			default:  lx_sql_ungetc(lx, c); return TOK_CHAR;
			}

		case S4: /* e.g. "]" */
			lx_sql_ungetc(lx, c); return lx->z = z1, TOK_CLOSEGROUP;

		case S5: /* e.g. "^" */
			lx_sql_ungetc(lx, c); return TOK_INVERT;

		case S6: /* e.g. "[:" */
			switch (c) {
			case 'A': state = S7; continue;
			case 'D': state = S8; continue;
			case 'L': state = S9; continue;
			case 'S': state = S10; continue;
			case 'U': state = S11; continue;
			case 'W': state = S12; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S7: /* e.g. "[:A" */
			switch (c) {
			case 'L': state = S48; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S8: /* e.g. "[:D" */
			switch (c) {
			case 'I': state = S14; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S9: /* e.g. "[:L" */
			switch (c) {
			case 'O': state = S18; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S10: /* e.g. "[:S" */
			switch (c) {
			case 'P': state = S13; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S11: /* e.g. "[:U" */
			switch (c) {
			case 'P': state = S35; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S12: /* e.g. "[:W" */
			switch (c) {
			case 'H': state = S16; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S13: /* e.g. "[:SP" */
			switch (c) {
			case 'A': state = S41; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S14: /* e.g. "[:DI" */
			switch (c) {
			case 'G': state = S15; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S15: /* e.g. "[:DIG" */
			switch (c) {
			case 'I': state = S20; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S16: /* e.g. "[:WH" */
			switch (c) {
			case 'I': state = S17; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S17: /* e.g. "[:WHI" */
			switch (c) {
			case 'T': state = S22; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S18: /* e.g. "[:LO" */
			switch (c) {
			case 'W': state = S19; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S19: /* e.g. "[:LOW" */
			switch (c) {
			case 'E': state = S28; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S20: /* e.g. "[:DIGI" */
			switch (c) {
			case 'T': state = S21; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S21: /* e.g. "[:DIGIT" */
			switch (c) {
			case ':': state = S46; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S22: /* e.g. "[:WHIT" */
			switch (c) {
			case 'E': state = S23; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S23: /* e.g. "[:WHITE" */
			switch (c) {
			case 'S': state = S24; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S24: /* e.g. "[:WHITES" */
			switch (c) {
			case 'P': state = S25; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S25: /* e.g. "[:WHITESP" */
			switch (c) {
			case 'A': state = S26; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S26: /* e.g. "[:WHITESPA" */
			switch (c) {
			case 'C': state = S27; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S27: /* e.g. "[:WHITESPAC" */
			switch (c) {
			case 'E': state = S30; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S28: /* e.g. "[:LOWE" */
			switch (c) {
			case 'R': state = S29; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S29: /* e.g. "[:LOWER" */
			switch (c) {
			case ':': state = S33; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S30: /* e.g. "[:WHITESPACE" */
			switch (c) {
			case ':': state = S31; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S31: /* e.g. "[:WHITESPACE:" */
			switch (c) {
			case ']': state = S32; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S32: /* e.g. "[:WHITESPACE:]" */
			lx_sql_ungetc(lx, c); return TOK_CLASS_SPACE;

		case S33: /* e.g. "[:LOWER:" */
			switch (c) {
			case ']': state = S34; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S34: /* e.g. "[:LOWER:]" */
			lx_sql_ungetc(lx, c); return TOK_CLASS_LOWER;

		case S35: /* e.g. "[:UP" */
			switch (c) {
			case 'P': state = S36; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S36: /* e.g. "[:UPP" */
			switch (c) {
			case 'E': state = S37; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S37: /* e.g. "[:UPPE" */
			switch (c) {
			case 'R': state = S38; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S38: /* e.g. "[:UPPER" */
			switch (c) {
			case ':': state = S39; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S39: /* e.g. "[:UPPER:" */
			switch (c) {
			case ']': state = S40; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S40: /* e.g. "[:UPPER:]" */
			lx_sql_ungetc(lx, c); return TOK_CLASS_UPPER;

		case S41: /* e.g. "[:SPA" */
			switch (c) {
			case 'C': state = S42; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S42: /* e.g. "[:SPAC" */
			switch (c) {
			case 'E': state = S43; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S43: /* e.g. "[:SPACE" */
			switch (c) {
			case ':': state = S44; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S44: /* e.g. "[:SPACE:" */
			switch (c) {
			case ']': state = S45; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S45: /* e.g. "[:SPACE:]" */
			lx_sql_ungetc(lx, c); return TOK_CLASS_SPCHR;

		case S46: /* e.g. "[:DIGIT:" */
			switch (c) {
			case ']': state = S47; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S47: /* e.g. "[:DIGIT:]" */
			lx_sql_ungetc(lx, c); return TOK_CLASS_DIGIT;

		case S48: /* e.g. "[:AL" */
			switch (c) {
			case 'N': state = S49; continue;
			case 'P': state = S50; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S49: /* e.g. "[:ALN" */
			switch (c) {
			case 'U': state = S51; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S50: /* e.g. "[:ALP" */
			switch (c) {
			case 'H': state = S55; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S51: /* e.g. "[:ALNU" */
			switch (c) {
			case 'M': state = S52; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S52: /* e.g. "[:ALNUM" */
			switch (c) {
			case ':': state = S53; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S53: /* e.g. "[:ALNUM:" */
			switch (c) {
			case ']': state = S54; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S54: /* e.g. "[:ALNUM:]" */
			lx_sql_ungetc(lx, c); return TOK_CLASS_ALNUM;

		case S55: /* e.g. "[:ALPH" */
			switch (c) {
			case 'A': state = S56; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S56: /* e.g. "[:ALPHA" */
			switch (c) {
			case ':': state = S57; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S57: /* e.g. "[:ALPHA:" */
			switch (c) {
			case ']': state = S58; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S58: /* e.g. "[:ALPHA:]" */
			lx_sql_ungetc(lx, c); return TOK_CLASS_ALPHA;
		}
	}

	lx->lgetc = NULL;

	switch (state) {
	case S1: return TOK_CHAR;
	case S2: return TOK_RANGE;
	case S3: return TOK_CHAR;
	case S4: return TOK_CLOSEGROUP;
	case S5: return TOK_INVERT;
	case S32: return TOK_CLASS_SPACE;
	case S34: return TOK_CLASS_LOWER;
	case S40: return TOK_CLASS_UPPER;
	case S45: return TOK_CLASS_SPCHR;
	case S47: return TOK_CLASS_DIGIT;
	case S54: return TOK_CLASS_ALNUM;
	case S58: return TOK_CLASS_ALPHA;
	default: errno = EINVAL; return TOK_ERROR;
	}
}

static enum lx_sql_token
z1(struct lx_sql_lx *lx)
{
	int c;

	enum {
		S0, S1, S2, S3, S4, S5, S6, S7, S8, S9, 
		S10
	} state;

	assert(lx != NULL);

	if (lx->clear != NULL) {
		lx->clear(lx);
	}

	state = S0;

	lx->start = lx->end;

	while (c = lx_getc(lx), c != EOF) {
		if (lx->push != NULL) {
			if (-1 == lx->push(lx, c)) {
				return TOK_ERROR;
			}
		}

		switch (state) {
		case S0: /* start */
			switch (c) {
			case '\x00':
			case '\x01':
			case '\x02':
			case '\x03':
			case '\x04':
			case '\x05':
			case '\x06':
			case '\a':
			case '\b':
			case '\t':
			case '\n':
			case '\v':
			case '\f':
			case '\r':
			case '\x0e':
			case '\x0f':
			case '\x10':
			case '\x11':
			case '\x12':
			case '\x13':
			case '\x14':
			case '\x15':
			case '\x16':
			case '\x17':
			case '\x18':
			case '\x19':
			case '\x1a':
			case '\x1b':
			case '\x1c':
			case '\x1d':
			case '\x1e':
			case '\x1f':
			case ' ':
			case '!':
			case '"':
			case '#':
			case '$': state = S1; continue;
			case '%': state = S2; continue;
			case '&':
			case '\'': state = S1; continue;
			case '(': state = S3; continue;
			case ')': state = S4; continue;
			case '*': state = S5; continue;
			case '+': state = S6; continue;
			case ',':
			case '-':
			case '.':
			case '\x2f':
			case '0':
			case '1':
			case '2':
			case '3':
			case '4':
			case '5':
			case '6':
			case '7':
			case '8':
			case '9':
			case ':':
			case ';':
			case '<':
			case '=':
			case '>': state = S1; continue;
			case '?': state = S7; continue;
			case '@':
			case 'A':
			case 'B':
			case 'C':
			case 'D':
			case 'E':
			case 'F':
			case 'G':
			case 'H':
			case 'I':
			case 'J':
			case 'K':
			case 'L':
			case 'M':
			case 'N':
			case 'O':
			case 'P':
			case 'Q':
			case 'R':
			case 'S':
			case 'T':
			case 'U':
			case 'V':
			case 'W':
			case 'X':
			case 'Y':
			case 'Z': state = S1; continue;
			case '[': state = S8; continue;
			case '\\':
			case ']':
			case '^': state = S1; continue;
			case '_': state = S9; continue;
			case '`':
			case 'a':
			case 'b':
			case 'c':
			case 'd':
			case 'e':
			case 'f':
			case 'g':
			case 'h':
			case 'i':
			case 'j':
			case 'k':
			case 'l':
			case 'm':
			case 'n':
			case 'o':
			case 'p':
			case 'q':
			case 'r':
			case 's':
			case 't':
			case 'u':
			case 'v':
			case 'w':
			case 'x':
			case 'y':
			case 'z':
			case '{': state = S1; continue;
			case '|': state = S10; continue;
			case '}':
			case '~':
			case '\x7f':
			case '\x80':
			case '\x81':
			case '\x82':
			case '\x83':
			case '\x84':
			case '\x85':
			case '\x86':
			case '\x87':
			case '\x88':
			case '\x89':
			case '\x8a':
			case '\x8b':
			case '\x8c':
			case '\x8d':
			case '\x8e':
			case '\x8f':
			case '\x90':
			case '\x91':
			case '\x92':
			case '\x93':
			case '\x94':
			case '\x95':
			case '\x96':
			case '\x97':
			case '\x98':
			case '\x99':
			case '\x9a':
			case '\x9b':
			case '\x9c':
			case '\x9d':
			case '\x9e':
			case '\x9f':
			case '\xa0':
			case '\xa1':
			case '\xa2':
			case '\xa3':
			case '\xa4':
			case '\xa5':
			case '\xa6':
			case '\xa7':
			case '\xa8':
			case '\xa9':
			case '\xaa':
			case '\xab':
			case '\xac':
			case '\xad':
			case '\xae':
			case '\xaf':
			case '\xb0':
			case '\xb1':
			case '\xb2':
			case '\xb3':
			case '\xb4':
			case '\xb5':
			case '\xb6':
			case '\xb7':
			case '\xb8':
			case '\xb9':
			case '\xba':
			case '\xbb':
			case '\xbc':
			case '\xbd':
			case '\xbe':
			case '\xbf':
			case '\xc0':
			case '\xc1':
			case '\xc2':
			case '\xc3':
			case '\xc4':
			case '\xc5':
			case '\xc6':
			case '\xc7':
			case '\xc8':
			case '\xc9':
			case '\xca':
			case '\xcb':
			case '\xcc':
			case '\xcd':
			case '\xce':
			case '\xcf':
			case '\xd0':
			case '\xd1':
			case '\xd2':
			case '\xd3':
			case '\xd4':
			case '\xd5':
			case '\xd6':
			case '\xd7':
			case '\xd8':
			case '\xd9':
			case '\xda':
			case '\xdb':
			case '\xdc':
			case '\xdd':
			case '\xde':
			case '\xdf':
			case '\xe0':
			case '\xe1':
			case '\xe2':
			case '\xe3':
			case '\xe4':
			case '\xe5':
			case '\xe6':
			case '\xe7':
			case '\xe8':
			case '\xe9':
			case '\xea':
			case '\xeb':
			case '\xec':
			case '\xed':
			case '\xee':
			case '\xef':
			case '\xf0':
			case '\xf1':
			case '\xf2':
			case '\xf3':
			case '\xf4':
			case '\xf5':
			case '\xf6':
			case '\xf7':
			case '\xf8':
			case '\xf9':
			case '\xfa':
			case '\xfb':
			case '\xfc':
			case '\xfd':
			case '\xfe':
			case '\xff': state = S1; continue;
			default:  lx->lgetc = NULL; return TOK_UNKNOWN;
			}

		case S1: /* e.g. "a" */
			lx_sql_ungetc(lx, c); return TOK_CHAR;

		case S2: /* e.g. "%" */
			lx_sql_ungetc(lx, c); return TOK_MANY;

		case S3: /* e.g. "(" */
			lx_sql_ungetc(lx, c); return TOK_OPENSUB;

		case S4: /* e.g. ")" */
			lx_sql_ungetc(lx, c); return TOK_CLOSESUB;

		case S5: /* e.g. "*" */
			lx_sql_ungetc(lx, c); return TOK_STAR;

		case S6: /* e.g. "+" */
			lx_sql_ungetc(lx, c); return TOK_PLUS;

		case S7: /* e.g. "?" */
			lx_sql_ungetc(lx, c); return TOK_OPT;

		case S8: /* e.g. "[" */
			lx_sql_ungetc(lx, c); return lx->z = z0, TOK_OPENGROUP;

		case S9: /* e.g. "_" */
			lx_sql_ungetc(lx, c); return TOK_ANY;

		case S10: /* e.g. "|" */
			lx_sql_ungetc(lx, c); return TOK_ALT;
		}
	}

	lx->lgetc = NULL;

	switch (state) {
	case S1: return TOK_CHAR;
	case S2: return TOK_MANY;
	case S3: return TOK_OPENSUB;
	case S4: return TOK_CLOSESUB;
	case S5: return TOK_STAR;
	case S6: return TOK_PLUS;
	case S7: return TOK_OPT;
	case S8: return TOK_OPENGROUP;
	case S9: return TOK_ANY;
	case S10: return TOK_ALT;
	default: errno = EINVAL; return TOK_ERROR;
	}
}

const char *
lx_sql_name(enum lx_sql_token t)
{
	switch (t) {
	case TOK_CHAR: return "CHAR";
	case TOK_CLASS_SPACE: return "CLASS_SPACE";
	case TOK_CLASS_UPPER: return "CLASS_UPPER";
	case TOK_CLASS_SPCHR: return "CLASS_SPCHR";
	case TOK_CLASS_LOWER: return "CLASS_LOWER";
	case TOK_CLASS_DIGIT: return "CLASS_DIGIT";
	case TOK_CLASS_ALPHA: return "CLASS_ALPHA";
	case TOK_CLASS_ALNUM: return "CLASS_ALNUM";
	case TOK_RANGE: return "RANGE";
	case TOK_INVERT: return "INVERT";
	case TOK_CLOSEGROUP: return "CLOSEGROUP";
	case TOK_OPENGROUP: return "OPENGROUP";
	case TOK_ALT: return "ALT";
	case TOK_PLUS: return "PLUS";
	case TOK_STAR: return "STAR";
	case TOK_OPT: return "OPT";
	case TOK_CLOSESUB: return "CLOSESUB";
	case TOK_OPENSUB: return "OPENSUB";
	case TOK_MANY: return "MANY";
	case TOK_ANY: return "ANY";
	case TOK_EOF:     return "EOF";
	case TOK_ERROR:   return "ERROR";
	case TOK_UNKNOWN: return "UNKNOWN";
	default: return "?";
	}
}

const char *
lx_sql_example(enum lx_sql_token (*z)(struct lx_sql_lx *), enum lx_sql_token t)
{
	assert(z != NULL);

	if (z == z0) {
		switch (t) {
		case TOK_CHAR: return "a";
		case TOK_CLASS_SPACE: return "[:WHITESPACE:]";
		case TOK_CLASS_UPPER: return "[:UPPER:]";
		case TOK_CLASS_SPCHR: return "[:SPACE:]";
		case TOK_CLASS_LOWER: return "[:LOWER:]";
		case TOK_CLASS_DIGIT: return "[:DIGIT:]";
		case TOK_CLASS_ALPHA: return "[:ALPHA:]";
		case TOK_CLASS_ALNUM: return "[:ALNUM:]";
		case TOK_RANGE: return "-";
		case TOK_INVERT: return "^";
		case TOK_CLOSEGROUP: return "]";
		default: goto error;
		}
	} else
	if (z == z1) {
		switch (t) {
		case TOK_CHAR: return "a";
		case TOK_OPENGROUP: return "[";
		case TOK_ALT: return "|";
		case TOK_PLUS: return "+";
		case TOK_STAR: return "*";
		case TOK_OPT: return "?";
		case TOK_CLOSESUB: return ")";
		case TOK_OPENSUB: return "(";
		case TOK_MANY: return "%";
		case TOK_ANY: return "_";
		default: goto error;
		}
	}

error:

	errno = EINVAL;
	return NULL;
}

void
lx_sql_init(struct lx_sql_lx *lx)
{
	static const struct lx_sql_lx lx_default;

	assert(lx != NULL);

	*lx = lx_default;

	lx->c = EOF;
	lx->z = z1;

	lx->end.byte = 0;
	lx->end.line = 1;
	lx->end.col  = 1;
}

enum lx_sql_token
lx_sql_next(struct lx_sql_lx *lx)
{
	enum lx_sql_token t;

	assert(lx != NULL);
	assert(lx->z != NULL);

	if (lx->lgetc == NULL) {
		return TOK_EOF;
	}

	t = lx->z(lx);

	if (lx->push != NULL) {
		if (-1 == lx->push(lx, '\0')) {
			return TOK_ERROR;
		}
	}

	return t;
}

