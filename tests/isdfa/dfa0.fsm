0  ->  1 "a"; # e.g. "a"
0  ->  0 "b"; # start
0  ->  2 "c"; # e.g. "c"
0  ->  3 "d"; # e.g. "d"
1  ->  2 "b"; # e.g. "c"
1  ->  5 "d"; # e.g. "ad"
2  ->  0 "b"; # start
2  ->  4 "d"; # e.g. "cd"
3  ->  3 "a"; # e.g. "d"
4  ->  3 "a"; # e.g. "d"
4  ->  7 "b"; # e.g. "cdb"
4  ->  8 "d"; # e.g. "cdd"
5  ->  6 "a"; # e.g. "ada"
5  ->  0 "b"; # start
5  ->  2 "c"; # e.g. "c"
5  ->  3 "d"; # e.g. "d"
6  ->  3 "a"; # e.g. "d"
6  ->  2 "b"; # e.g. "c"
6  ->  5 "d"; # e.g. "ad"
7  ->  1 "a"; # e.g. "a"
7  ->  0 "b"; # start
7  ->  2 "c"; # e.g. "c"
7  ->  4 "d"; # e.g. "cd"
8  ->  6 "a"; # e.g. "ada"
8  ->  7 "b"; # e.g. "cdb"
8  ->  2 "c"; # e.g. "c"
8  ->  8 "d"; # e.g. "cdd"

start: 0;
end:   0, 5, 7, 8;
