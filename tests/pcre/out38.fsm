0  ->  1 "\x00"; # e.g. "a"
0  ->  1 "\x01"; # e.g. "a"
0  ->  1 "\x02"; # e.g. "a"
0  ->  1 "\x03"; # e.g. "a"
0  ->  1 "\x04"; # e.g. "a"
0  ->  1 "\x05"; # e.g. "a"
0  ->  1 "\x06"; # e.g. "a"
0  ->  1 "\x07"; # e.g. "a"
0  ->  1 "\x08"; # e.g. "a"
0  ->  1 "\t"; # e.g. "a"
0  ->  1 "\n"; # e.g. "a"
0  ->  1 "\v"; # e.g. "a"
0  ->  1 "\f"; # e.g. "a"
0  ->  1 "\r"; # e.g. "a"
0  ->  1 "\x0e"; # e.g. "a"
0  ->  1 "\x0f"; # e.g. "a"
0  ->  1 "\x10"; # e.g. "a"
0  ->  1 "\x11"; # e.g. "a"
0  ->  1 "\x12"; # e.g. "a"
0  ->  1 "\x13"; # e.g. "a"
0  ->  1 "\x14"; # e.g. "a"
0  ->  1 "\x15"; # e.g. "a"
0  ->  1 "\x16"; # e.g. "a"
0  ->  1 "\x17"; # e.g. "a"
0  ->  1 "\x18"; # e.g. "a"
0  ->  1 "\x19"; # e.g. "a"
0  ->  1 "\x1a"; # e.g. "a"
0  ->  1 "\x1b"; # e.g. "a"
0  ->  1 "\x1c"; # e.g. "a"
0  ->  1 "\x1d"; # e.g. "a"
0  ->  1 "\x1e"; # e.g. "a"
0  ->  1 "\x1f"; # e.g. "a"
0  ->  1 " "; # e.g. "a"
0  ->  1 "!"; # e.g. "a"
0  ->  1 "\""; # e.g. "a"
0  ->  1 "#"; # e.g. "a"
0  ->  1 "$"; # e.g. "a"
0  ->  1 "%"; # e.g. "a"
0  ->  1 "&"; # e.g. "a"
0  ->  1 "'"; # e.g. "a"
0  ->  1 "("; # e.g. "a"
0  ->  1 ")"; # e.g. "a"
0  ->  1 "*"; # e.g. "a"
0  ->  1 "+"; # e.g. "a"
0  ->  1 ","; # e.g. "a"
0  ->  1 "1"; # e.g. "a"
0  ->  1 "2"; # e.g. "a"
0  ->  1 "3"; # e.g. "a"
0  ->  1 "4"; # e.g. "a"
0  ->  1 "5"; # e.g. "a"
0  ->  1 "6"; # e.g. "a"
0  ->  1 "7"; # e.g. "a"
0  ->  1 "8"; # e.g. "a"
0  ->  1 "9"; # e.g. "a"
0  ->  1 ":"; # e.g. "a"
0  ->  1 ";"; # e.g. "a"
0  ->  1 "<"; # e.g. "a"
0  ->  1 "="; # e.g. "a"
0  ->  1 ">"; # e.g. "a"
0  ->  1 "?"; # e.g. "a"
0  ->  1 "@"; # e.g. "a"
0  ->  1 "A"; # e.g. "a"
0  ->  1 "B"; # e.g. "a"
0  ->  1 "C"; # e.g. "a"
0  ->  1 "D"; # e.g. "a"
0  ->  1 "E"; # e.g. "a"
0  ->  1 "F"; # e.g. "a"
0  ->  1 "G"; # e.g. "a"
0  ->  1 "H"; # e.g. "a"
0  ->  1 "I"; # e.g. "a"
0  ->  1 "J"; # e.g. "a"
0  ->  1 "K"; # e.g. "a"
0  ->  1 "L"; # e.g. "a"
0  ->  1 "M"; # e.g. "a"
0  ->  1 "N"; # e.g. "a"
0  ->  1 "O"; # e.g. "a"
0  ->  1 "P"; # e.g. "a"
0  ->  1 "Q"; # e.g. "a"
0  ->  1 "R"; # e.g. "a"
0  ->  1 "S"; # e.g. "a"
0  ->  1 "T"; # e.g. "a"
0  ->  1 "U"; # e.g. "a"
0  ->  1 "V"; # e.g. "a"
0  ->  1 "W"; # e.g. "a"
0  ->  1 "X"; # e.g. "a"
0  ->  1 "Y"; # e.g. "a"
0  ->  1 "Z"; # e.g. "a"
0  ->  1 "["; # e.g. "a"
0  ->  1 "\\"; # e.g. "a"
0  ->  1 "]"; # e.g. "a"
0  ->  1 "^"; # e.g. "a"
0  ->  1 "_"; # e.g. "a"
0  ->  1 "`"; # e.g. "a"
0  ->  1 "a"; # e.g. "a"
0  ->  1 "b"; # e.g. "a"
0  ->  1 "c"; # e.g. "a"
0  ->  1 "d"; # e.g. "a"
0  ->  1 "e"; # e.g. "a"
0  ->  1 "f"; # e.g. "a"
0  ->  1 "g"; # e.g. "a"
0  ->  1 "h"; # e.g. "a"
0  ->  1 "i"; # e.g. "a"
0  ->  1 "j"; # e.g. "a"
0  ->  1 "k"; # e.g. "a"
0  ->  1 "l"; # e.g. "a"
0  ->  1 "m"; # e.g. "a"
0  ->  1 "n"; # e.g. "a"
0  ->  1 "o"; # e.g. "a"
0  ->  1 "p"; # e.g. "a"
0  ->  1 "q"; # e.g. "a"
0  ->  1 "r"; # e.g. "a"
0  ->  1 "s"; # e.g. "a"
0  ->  1 "t"; # e.g. "a"
0  ->  1 "u"; # e.g. "a"
0  ->  1 "v"; # e.g. "a"
0  ->  1 "w"; # e.g. "a"
0  ->  1 "x"; # e.g. "a"
0  ->  1 "y"; # e.g. "a"
0  ->  1 "z"; # e.g. "a"
0  ->  1 "{"; # e.g. "a"
0  ->  1 "|"; # e.g. "a"
0  ->  1 "}"; # e.g. "a"
0  ->  1 "~"; # e.g. "a"
0  ->  1 "\x7f"; # e.g. "a"
0  ->  1 "\x80"; # e.g. "a"
0  ->  1 "\x81"; # e.g. "a"
0  ->  1 "\x82"; # e.g. "a"
0  ->  1 "\x83"; # e.g. "a"
0  ->  1 "\x84"; # e.g. "a"
0  ->  1 "\x85"; # e.g. "a"
0  ->  1 "\x86"; # e.g. "a"
0  ->  1 "\x87"; # e.g. "a"
0  ->  1 "\x88"; # e.g. "a"
0  ->  1 "\x89"; # e.g. "a"
0  ->  1 "\x8a"; # e.g. "a"
0  ->  1 "\x8b"; # e.g. "a"
0  ->  1 "\x8c"; # e.g. "a"
0  ->  1 "\x8d"; # e.g. "a"
0  ->  1 "\x8e"; # e.g. "a"
0  ->  1 "\x8f"; # e.g. "a"
0  ->  1 "\x90"; # e.g. "a"
0  ->  1 "\x91"; # e.g. "a"
0  ->  1 "\x92"; # e.g. "a"
0  ->  1 "\x93"; # e.g. "a"
0  ->  1 "\x94"; # e.g. "a"
0  ->  1 "\x95"; # e.g. "a"
0  ->  1 "\x96"; # e.g. "a"
0  ->  1 "\x97"; # e.g. "a"
0  ->  1 "\x98"; # e.g. "a"
0  ->  1 "\x99"; # e.g. "a"
0  ->  1 "\x9a"; # e.g. "a"
0  ->  1 "\x9b"; # e.g. "a"
0  ->  1 "\x9c"; # e.g. "a"
0  ->  1 "\x9d"; # e.g. "a"
0  ->  1 "\x9e"; # e.g. "a"
0  ->  1 "\x9f"; # e.g. "a"
0  ->  1 "\xa0"; # e.g. "a"
0  ->  1 "\xa1"; # e.g. "a"
0  ->  1 "\xa2"; # e.g. "a"
0  ->  1 "\xa3"; # e.g. "a"
0  ->  1 "\xa4"; # e.g. "a"
0  ->  1 "\xa5"; # e.g. "a"
0  ->  1 "\xa6"; # e.g. "a"
0  ->  1 "\xa7"; # e.g. "a"
0  ->  1 "\xa8"; # e.g. "a"
0  ->  1 "\xa9"; # e.g. "a"
0  ->  1 "\xaa"; # e.g. "a"
0  ->  1 "\xab"; # e.g. "a"
0  ->  1 "\xac"; # e.g. "a"
0  ->  1 "\xad"; # e.g. "a"
0  ->  1 "\xae"; # e.g. "a"
0  ->  1 "\xaf"; # e.g. "a"
0  ->  1 "\xb0"; # e.g. "a"
0  ->  1 "\xb1"; # e.g. "a"
0  ->  1 "\xb2"; # e.g. "a"
0  ->  1 "\xb3"; # e.g. "a"
0  ->  1 "\xb4"; # e.g. "a"
0  ->  1 "\xb5"; # e.g. "a"
0  ->  1 "\xb6"; # e.g. "a"
0  ->  1 "\xb7"; # e.g. "a"
0  ->  1 "\xb8"; # e.g. "a"
0  ->  1 "\xb9"; # e.g. "a"
0  ->  1 "\xba"; # e.g. "a"
0  ->  1 "\xbb"; # e.g. "a"
0  ->  1 "\xbc"; # e.g. "a"
0  ->  1 "\xbd"; # e.g. "a"
0  ->  1 "\xbe"; # e.g. "a"
0  ->  1 "\xbf"; # e.g. "a"
0  ->  1 "\xc0"; # e.g. "a"
0  ->  1 "\xc1"; # e.g. "a"
0  ->  1 "\xc2"; # e.g. "a"
0  ->  1 "\xc3"; # e.g. "a"
0  ->  1 "\xc4"; # e.g. "a"
0  ->  1 "\xc5"; # e.g. "a"
0  ->  1 "\xc6"; # e.g. "a"
0  ->  1 "\xc7"; # e.g. "a"
0  ->  1 "\xc8"; # e.g. "a"
0  ->  1 "\xc9"; # e.g. "a"
0  ->  1 "\xca"; # e.g. "a"
0  ->  1 "\xcb"; # e.g. "a"
0  ->  1 "\xcc"; # e.g. "a"
0  ->  1 "\xcd"; # e.g. "a"
0  ->  1 "\xce"; # e.g. "a"
0  ->  1 "\xcf"; # e.g. "a"
0  ->  1 "\xd0"; # e.g. "a"
0  ->  1 "\xd1"; # e.g. "a"
0  ->  1 "\xd2"; # e.g. "a"
0  ->  1 "\xd3"; # e.g. "a"
0  ->  1 "\xd4"; # e.g. "a"
0  ->  1 "\xd5"; # e.g. "a"
0  ->  1 "\xd6"; # e.g. "a"
0  ->  1 "\xd7"; # e.g. "a"
0  ->  1 "\xd8"; # e.g. "a"
0  ->  1 "\xd9"; # e.g. "a"
0  ->  1 "\xda"; # e.g. "a"
0  ->  1 "\xdb"; # e.g. "a"
0  ->  1 "\xdc"; # e.g. "a"
0  ->  1 "\xdd"; # e.g. "a"
0  ->  1 "\xde"; # e.g. "a"
0  ->  1 "\xdf"; # e.g. "a"
0  ->  1 "\xe0"; # e.g. "a"
0  ->  1 "\xe1"; # e.g. "a"
0  ->  1 "\xe2"; # e.g. "a"
0  ->  1 "\xe3"; # e.g. "a"
0  ->  1 "\xe4"; # e.g. "a"
0  ->  1 "\xe5"; # e.g. "a"
0  ->  1 "\xe6"; # e.g. "a"
0  ->  1 "\xe7"; # e.g. "a"
0  ->  1 "\xe8"; # e.g. "a"
0  ->  1 "\xe9"; # e.g. "a"
0  ->  1 "\xea"; # e.g. "a"
0  ->  1 "\xeb"; # e.g. "a"
0  ->  1 "\xec"; # e.g. "a"
0  ->  1 "\xed"; # e.g. "a"
0  ->  1 "\xee"; # e.g. "a"
0  ->  1 "\xef"; # e.g. "a"
0  ->  1 "\xf0"; # e.g. "a"
0  ->  1 "\xf1"; # e.g. "a"
0  ->  1 "\xf2"; # e.g. "a"
0  ->  1 "\xf3"; # e.g. "a"
0  ->  1 "\xf4"; # e.g. "a"
0  ->  1 "\xf5"; # e.g. "a"
0  ->  1 "\xf6"; # e.g. "a"
0  ->  1 "\xf7"; # e.g. "a"
0  ->  1 "\xf8"; # e.g. "a"
0  ->  1 "\xf9"; # e.g. "a"
0  ->  1 "\xfa"; # e.g. "a"
0  ->  1 "\xfb"; # e.g. "a"
0  ->  1 "\xfc"; # e.g. "a"
0  ->  1 "\xfd"; # e.g. "a"
0  ->  1 "\xfe"; # e.g. "a"
0  ->  1 "\xff"; # e.g. "a"

1 -> 2 "\n";

start: 0;
end:   1, 2;
