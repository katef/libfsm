0  ->  1 "\x00"; # e.g. "g"
0  ->  1 "\x01"; # e.g. "g"
0  ->  1 "\x02"; # e.g. "g"
0  ->  1 "\x03"; # e.g. "g"
0  ->  1 "\x04"; # e.g. "g"
0  ->  1 "\x05"; # e.g. "g"
0  ->  1 "\x06"; # e.g. "g"
0  ->  2 "\x07"; # e.g. "gg\t"
0  ->  1 "\x08"; # e.g. "g"
0  ->  1 "\t"; # e.g. "g"
0  ->  3 "\n"; # e.g. "\n"
0  ->  1 "\v"; # e.g. "g"
0  ->  1 "\f"; # e.g. "g"
0  ->  1 "\r"; # e.g. "g"
0  ->  1 "\x0e"; # e.g. "g"
0  ->  1 "\x0f"; # e.g. "g"
0  ->  1 "\x10"; # e.g. "g"
0  ->  1 "\x11"; # e.g. "g"
0  ->  1 "\x12"; # e.g. "g"
0  ->  1 "\x13"; # e.g. "g"
0  ->  1 "\x14"; # e.g. "g"
0  ->  1 "\x15"; # e.g. "g"
0  ->  1 "\x16"; # e.g. "g"
0  ->  1 "\x17"; # e.g. "g"
0  ->  1 "\x18"; # e.g. "g"
0  ->  1 "\x19"; # e.g. "g"
0  ->  1 "\x1a"; # e.g. "g"
0  ->  1 "\x1b"; # e.g. "g"
0  ->  1 "\x1c"; # e.g. "g"
0  ->  1 "\x1d"; # e.g. "g"
0  ->  1 "\x1e"; # e.g. "g"
0  ->  1 "\x1f"; # e.g. "g"
0  ->  1 " "; # e.g. "g"
0  ->  1 "!"; # e.g. "g"
0  ->  1 "\""; # e.g. "g"
0  ->  1 "#"; # e.g. "g"
0  ->  1 "$"; # e.g. "g"
0  ->  1 "%"; # e.g. "g"
0  ->  1 "&"; # e.g. "g"
0  ->  1 "'"; # e.g. "g"
0  ->  1 "("; # e.g. "g"
0  ->  1 ")"; # e.g. "g"
0  ->  1 "*"; # e.g. "g"
0  ->  1 "+"; # e.g. "g"
0  ->  1 ","; # e.g. "g"
0  ->  1 "."; # e.g. "g"
0  ->  1 "/"; # e.g. "g"
0  ->  1 "0"; # e.g. "g"
0  ->  1 "1"; # e.g. "g"
0  ->  1 "2"; # e.g. "g"
0  ->  1 "3"; # e.g. "g"
0  ->  1 "4"; # e.g. "g"
0  ->  1 "5"; # e.g. "g"
0  ->  1 "6"; # e.g. "g"
0  ->  1 "7"; # e.g. "g"
0  ->  1 "8"; # e.g. "g"
0  ->  1 "9"; # e.g. "g"
0  ->  1 ":"; # e.g. "g"
0  ->  1 ";"; # e.g. "g"
0  ->  1 "<"; # e.g. "g"
0  ->  1 "="; # e.g. "g"
0  ->  1 ">"; # e.g. "g"
0  ->  1 "?"; # e.g. "g"
0  ->  1 "@"; # e.g. "g"
0  ->  1 "A"; # e.g. "g"
0  ->  1 "B"; # e.g. "g"
0  ->  1 "C"; # e.g. "g"
0  ->  1 "D"; # e.g. "g"
0  ->  1 "E"; # e.g. "g"
0  ->  1 "F"; # e.g. "g"
0  ->  1 "G"; # e.g. "g"
0  ->  1 "H"; # e.g. "g"
0  ->  1 "I"; # e.g. "g"
0  ->  1 "J"; # e.g. "g"
0  ->  1 "K"; # e.g. "g"
0  ->  1 "L"; # e.g. "g"
0  ->  1 "M"; # e.g. "g"
0  ->  1 "N"; # e.g. "g"
0  ->  1 "O"; # e.g. "g"
0  ->  1 "P"; # e.g. "g"
0  ->  1 "Q"; # e.g. "g"
0  ->  1 "R"; # e.g. "g"
0  ->  1 "S"; # e.g. "g"
0  ->  1 "T"; # e.g. "g"
0  ->  1 "U"; # e.g. "g"
0  ->  1 "V"; # e.g. "g"
0  ->  1 "W"; # e.g. "g"
0  ->  1 "X"; # e.g. "g"
0  ->  1 "Y"; # e.g. "g"
0  ->  1 "Z"; # e.g. "g"
0  ->  1 "["; # e.g. "g"
0  ->  1 "\\"; # e.g. "g"
0  ->  1 "]"; # e.g. "g"
0  ->  1 "_"; # e.g. "g"
0  ->  1 "`"; # e.g. "g"
0  ->  1 "g"; # e.g. "g"
0  ->  1 "h"; # e.g. "g"
0  ->  1 "i"; # e.g. "g"
0  ->  1 "j"; # e.g. "g"
0  ->  1 "k"; # e.g. "g"
0  ->  1 "l"; # e.g. "g"
0  ->  1 "m"; # e.g. "g"
0  ->  1 "n"; # e.g. "g"
0  ->  1 "o"; # e.g. "g"
0  ->  1 "p"; # e.g. "g"
0  ->  1 "q"; # e.g. "g"
0  ->  1 "r"; # e.g. "g"
0  ->  1 "s"; # e.g. "g"
0  ->  1 "t"; # e.g. "g"
0  ->  1 "u"; # e.g. "g"
0  ->  1 "v"; # e.g. "g"
0  ->  1 "w"; # e.g. "g"
0  ->  1 "x"; # e.g. "g"
0  ->  1 "y"; # e.g. "g"
0  ->  1 "z"; # e.g. "g"
0  ->  1 "{"; # e.g. "g"
0  ->  1 "|"; # e.g. "g"
0  ->  1 "}"; # e.g. "g"
0  ->  1 "~"; # e.g. "g"
0  ->  1 "\x7f"; # e.g. "g"
0  ->  1 "\x80"; # e.g. "g"
0  ->  1 "\x81"; # e.g. "g"
0  ->  1 "\x82"; # e.g. "g"
0  ->  1 "\x83"; # e.g. "g"
0  ->  1 "\x84"; # e.g. "g"
0  ->  1 "\x85"; # e.g. "g"
0  ->  1 "\x86"; # e.g. "g"
0  ->  1 "\x87"; # e.g. "g"
0  ->  1 "\x88"; # e.g. "g"
0  ->  1 "\x89"; # e.g. "g"
0  ->  1 "\x8a"; # e.g. "g"
0  ->  1 "\x8b"; # e.g. "g"
0  ->  1 "\x8c"; # e.g. "g"
0  ->  1 "\x8d"; # e.g. "g"
0  ->  1 "\x8e"; # e.g. "g"
0  ->  1 "\x8f"; # e.g. "g"
0  ->  1 "\x90"; # e.g. "g"
0  ->  1 "\x91"; # e.g. "g"
0  ->  1 "\x92"; # e.g. "g"
0  ->  1 "\x93"; # e.g. "g"
0  ->  1 "\x94"; # e.g. "g"
0  ->  1 "\x95"; # e.g. "g"
0  ->  1 "\x96"; # e.g. "g"
0  ->  1 "\x97"; # e.g. "g"
0  ->  1 "\x98"; # e.g. "g"
0  ->  1 "\x99"; # e.g. "g"
0  ->  1 "\x9a"; # e.g. "g"
0  ->  1 "\x9b"; # e.g. "g"
0  ->  1 "\x9c"; # e.g. "g"
0  ->  1 "\x9d"; # e.g. "g"
0  ->  1 "\x9e"; # e.g. "g"
0  ->  1 "\x9f"; # e.g. "g"
0  ->  1 "\xa0"; # e.g. "g"
0  ->  1 "\xa1"; # e.g. "g"
0  ->  1 "\xa2"; # e.g. "g"
0  ->  1 "\xa3"; # e.g. "g"
0  ->  1 "\xa4"; # e.g. "g"
0  ->  1 "\xa5"; # e.g. "g"
0  ->  1 "\xa6"; # e.g. "g"
0  ->  1 "\xa7"; # e.g. "g"
0  ->  1 "\xa8"; # e.g. "g"
0  ->  1 "\xa9"; # e.g. "g"
0  ->  1 "\xaa"; # e.g. "g"
0  ->  1 "\xab"; # e.g. "g"
0  ->  1 "\xac"; # e.g. "g"
0  ->  1 "\xad"; # e.g. "g"
0  ->  1 "\xae"; # e.g. "g"
0  ->  1 "\xaf"; # e.g. "g"
0  ->  1 "\xb0"; # e.g. "g"
0  ->  1 "\xb1"; # e.g. "g"
0  ->  1 "\xb2"; # e.g. "g"
0  ->  1 "\xb3"; # e.g. "g"
0  ->  1 "\xb4"; # e.g. "g"
0  ->  1 "\xb5"; # e.g. "g"
0  ->  1 "\xb6"; # e.g. "g"
0  ->  1 "\xb7"; # e.g. "g"
0  ->  1 "\xb8"; # e.g. "g"
0  ->  1 "\xb9"; # e.g. "g"
0  ->  1 "\xba"; # e.g. "g"
0  ->  1 "\xbb"; # e.g. "g"
0  ->  1 "\xbc"; # e.g. "g"
0  ->  1 "\xbd"; # e.g. "g"
0  ->  1 "\xbe"; # e.g. "g"
0  ->  1 "\xbf"; # e.g. "g"
0  ->  1 "\xc0"; # e.g. "g"
0  ->  1 "\xc1"; # e.g. "g"
0  ->  1 "\xc2"; # e.g. "g"
0  ->  1 "\xc3"; # e.g. "g"
0  ->  1 "\xc4"; # e.g. "g"
0  ->  1 "\xc5"; # e.g. "g"
0  ->  1 "\xc6"; # e.g. "g"
0  ->  1 "\xc7"; # e.g. "g"
0  ->  1 "\xc8"; # e.g. "g"
0  ->  1 "\xc9"; # e.g. "g"
0  ->  1 "\xca"; # e.g. "g"
0  ->  1 "\xcb"; # e.g. "g"
0  ->  1 "\xcc"; # e.g. "g"
0  ->  1 "\xcd"; # e.g. "g"
0  ->  1 "\xce"; # e.g. "g"
0  ->  1 "\xcf"; # e.g. "g"
0  ->  1 "\xd0"; # e.g. "g"
0  ->  1 "\xd1"; # e.g. "g"
0  ->  1 "\xd2"; # e.g. "g"
0  ->  1 "\xd3"; # e.g. "g"
0  ->  1 "\xd4"; # e.g. "g"
0  ->  1 "\xd5"; # e.g. "g"
0  ->  1 "\xd6"; # e.g. "g"
0  ->  1 "\xd7"; # e.g. "g"
0  ->  1 "\xd8"; # e.g. "g"
0  ->  1 "\xd9"; # e.g. "g"
0  ->  1 "\xda"; # e.g. "g"
0  ->  1 "\xdb"; # e.g. "g"
0  ->  1 "\xdc"; # e.g. "g"
0  ->  1 "\xdd"; # e.g. "g"
0  ->  1 "\xde"; # e.g. "g"
0  ->  1 "\xdf"; # e.g. "g"
0  ->  1 "\xe0"; # e.g. "g"
0  ->  1 "\xe1"; # e.g. "g"
0  ->  1 "\xe2"; # e.g. "g"
0  ->  1 "\xe3"; # e.g. "g"
0  ->  1 "\xe4"; # e.g. "g"
0  ->  1 "\xe5"; # e.g. "g"
0  ->  1 "\xe6"; # e.g. "g"
0  ->  1 "\xe7"; # e.g. "g"
0  ->  1 "\xe8"; # e.g. "g"
0  ->  1 "\xe9"; # e.g. "g"
0  ->  1 "\xea"; # e.g. "g"
0  ->  1 "\xeb"; # e.g. "g"
0  ->  1 "\xec"; # e.g. "g"
0  ->  1 "\xed"; # e.g. "g"
0  ->  1 "\xee"; # e.g. "g"
0  ->  1 "\xef"; # e.g. "g"
0  ->  1 "\xf0"; # e.g. "g"
0  ->  1 "\xf1"; # e.g. "g"
0  ->  1 "\xf2"; # e.g. "g"
0  ->  1 "\xf3"; # e.g. "g"
0  ->  1 "\xf4"; # e.g. "g"
0  ->  1 "\xf5"; # e.g. "g"
0  ->  1 "\xf6"; # e.g. "g"
0  ->  1 "\xf7"; # e.g. "g"
0  ->  1 "\xf8"; # e.g. "g"
0  ->  1 "\xf9"; # e.g. "g"
0  ->  1 "\xfa"; # e.g. "g"
0  ->  1 "\xfb"; # e.g. "g"
0  ->  1 "\xfc"; # e.g. "g"
0  ->  1 "\xfd"; # e.g. "g"
0  ->  1 "\xfe"; # e.g. "g"
1  ->  5 "\x00"; # e.g. "gg"
1  ->  5 "\x01"; # e.g. "gg"
1  ->  5 "\x02"; # e.g. "gg"
1  ->  5 "\x03"; # e.g. "gg"
1  ->  5 "\x04"; # e.g. "gg"
1  ->  5 "\x05"; # e.g. "gg"
1  ->  5 "\x06"; # e.g. "gg"
1  ->  2 "\x07"; # e.g. "gg\t"
1  ->  5 "\x08"; # e.g. "gg"
1  ->  5 "\t"; # e.g. "gg"
1  ->  6 "\n"; # e.g. "g\n"
1  ->  5 "\v"; # e.g. "gg"
1  ->  5 "\f"; # e.g. "gg"
1  ->  5 "\r"; # e.g. "gg"
1  ->  5 "\x0e"; # e.g. "gg"
1  ->  5 "\x0f"; # e.g. "gg"
1  ->  5 "\x10"; # e.g. "gg"
1  ->  5 "\x11"; # e.g. "gg"
1  ->  5 "\x12"; # e.g. "gg"
1  ->  5 "\x13"; # e.g. "gg"
1  ->  5 "\x14"; # e.g. "gg"
1  ->  5 "\x15"; # e.g. "gg"
1  ->  5 "\x16"; # e.g. "gg"
1  ->  5 "\x17"; # e.g. "gg"
1  ->  5 "\x18"; # e.g. "gg"
1  ->  5 "\x19"; # e.g. "gg"
1  ->  5 "\x1a"; # e.g. "gg"
1  ->  5 "\x1b"; # e.g. "gg"
1  ->  5 "\x1c"; # e.g. "gg"
1  ->  5 "\x1d"; # e.g. "gg"
1  ->  5 "\x1e"; # e.g. "gg"
1  ->  5 "\x1f"; # e.g. "gg"
1  ->  5 " "; # e.g. "gg"
1  ->  5 "!"; # e.g. "gg"
1  ->  5 "\""; # e.g. "gg"
1  ->  5 "#"; # e.g. "gg"
1  ->  5 "$"; # e.g. "gg"
1  ->  5 "%"; # e.g. "gg"
1  ->  5 "&"; # e.g. "gg"
1  ->  5 "'"; # e.g. "gg"
1  ->  5 "("; # e.g. "gg"
1  ->  5 ")"; # e.g. "gg"
1  ->  5 "*"; # e.g. "gg"
1  ->  5 "+"; # e.g. "gg"
1  ->  5 ","; # e.g. "gg"
1  ->  5 "."; # e.g. "gg"
1  ->  5 "/"; # e.g. "gg"
1  ->  5 "0"; # e.g. "gg"
1  ->  5 "1"; # e.g. "gg"
1  ->  5 "2"; # e.g. "gg"
1  ->  5 "3"; # e.g. "gg"
1  ->  5 "4"; # e.g. "gg"
1  ->  5 "5"; # e.g. "gg"
1  ->  5 "6"; # e.g. "gg"
1  ->  5 "7"; # e.g. "gg"
1  ->  5 "8"; # e.g. "gg"
1  ->  5 "9"; # e.g. "gg"
1  ->  5 ":"; # e.g. "gg"
1  ->  5 ";"; # e.g. "gg"
1  ->  5 "<"; # e.g. "gg"
1  ->  5 "="; # e.g. "gg"
1  ->  5 ">"; # e.g. "gg"
1  ->  5 "?"; # e.g. "gg"
1  ->  5 "@"; # e.g. "gg"
1  ->  5 "A"; # e.g. "gg"
1  ->  5 "B"; # e.g. "gg"
1  ->  5 "C"; # e.g. "gg"
1  ->  5 "D"; # e.g. "gg"
1  ->  5 "E"; # e.g. "gg"
1  ->  5 "F"; # e.g. "gg"
1  ->  5 "G"; # e.g. "gg"
1  ->  5 "H"; # e.g. "gg"
1  ->  5 "I"; # e.g. "gg"
1  ->  5 "J"; # e.g. "gg"
1  ->  5 "K"; # e.g. "gg"
1  ->  5 "L"; # e.g. "gg"
1  ->  5 "M"; # e.g. "gg"
1  ->  5 "N"; # e.g. "gg"
1  ->  5 "O"; # e.g. "gg"
1  ->  5 "P"; # e.g. "gg"
1  ->  5 "Q"; # e.g. "gg"
1  ->  5 "R"; # e.g. "gg"
1  ->  5 "S"; # e.g. "gg"
1  ->  5 "T"; # e.g. "gg"
1  ->  5 "U"; # e.g. "gg"
1  ->  5 "V"; # e.g. "gg"
1  ->  5 "W"; # e.g. "gg"
1  ->  5 "X"; # e.g. "gg"
1  ->  5 "Y"; # e.g. "gg"
1  ->  5 "Z"; # e.g. "gg"
1  ->  5 "["; # e.g. "gg"
1  ->  5 "\\"; # e.g. "gg"
1  ->  5 "]"; # e.g. "gg"
1  ->  5 "_"; # e.g. "gg"
1  ->  5 "`"; # e.g. "gg"
1  ->  5 "g"; # e.g. "gg"
1  ->  5 "h"; # e.g. "gg"
1  ->  5 "i"; # e.g. "gg"
1  ->  5 "j"; # e.g. "gg"
1  ->  5 "k"; # e.g. "gg"
1  ->  5 "l"; # e.g. "gg"
1  ->  5 "m"; # e.g. "gg"
1  ->  5 "n"; # e.g. "gg"
1  ->  5 "o"; # e.g. "gg"
1  ->  5 "p"; # e.g. "gg"
1  ->  5 "q"; # e.g. "gg"
1  ->  5 "r"; # e.g. "gg"
1  ->  5 "s"; # e.g. "gg"
1  ->  5 "t"; # e.g. "gg"
1  ->  5 "u"; # e.g. "gg"
1  ->  5 "v"; # e.g. "gg"
1  ->  5 "w"; # e.g. "gg"
1  ->  5 "x"; # e.g. "gg"
1  ->  5 "y"; # e.g. "gg"
1  ->  5 "z"; # e.g. "gg"
1  ->  5 "{"; # e.g. "gg"
1  ->  5 "|"; # e.g. "gg"
1  ->  5 "}"; # e.g. "gg"
1  ->  5 "~"; # e.g. "gg"
1  ->  5 "\x7f"; # e.g. "gg"
1  ->  5 "\x80"; # e.g. "gg"
1  ->  5 "\x81"; # e.g. "gg"
1  ->  5 "\x82"; # e.g. "gg"
1  ->  5 "\x83"; # e.g. "gg"
1  ->  5 "\x84"; # e.g. "gg"
1  ->  5 "\x85"; # e.g. "gg"
1  ->  5 "\x86"; # e.g. "gg"
1  ->  5 "\x87"; # e.g. "gg"
1  ->  5 "\x88"; # e.g. "gg"
1  ->  5 "\x89"; # e.g. "gg"
1  ->  5 "\x8a"; # e.g. "gg"
1  ->  5 "\x8b"; # e.g. "gg"
1  ->  5 "\x8c"; # e.g. "gg"
1  ->  5 "\x8d"; # e.g. "gg"
1  ->  5 "\x8e"; # e.g. "gg"
1  ->  5 "\x8f"; # e.g. "gg"
1  ->  5 "\x90"; # e.g. "gg"
1  ->  5 "\x91"; # e.g. "gg"
1  ->  5 "\x92"; # e.g. "gg"
1  ->  5 "\x93"; # e.g. "gg"
1  ->  5 "\x94"; # e.g. "gg"
1  ->  5 "\x95"; # e.g. "gg"
1  ->  5 "\x96"; # e.g. "gg"
1  ->  5 "\x97"; # e.g. "gg"
1  ->  5 "\x98"; # e.g. "gg"
1  ->  5 "\x99"; # e.g. "gg"
1  ->  5 "\x9a"; # e.g. "gg"
1  ->  5 "\x9b"; # e.g. "gg"
1  ->  5 "\x9c"; # e.g. "gg"
1  ->  5 "\x9d"; # e.g. "gg"
1  ->  5 "\x9e"; # e.g. "gg"
1  ->  5 "\x9f"; # e.g. "gg"
1  ->  5 "\xa0"; # e.g. "gg"
1  ->  5 "\xa1"; # e.g. "gg"
1  ->  5 "\xa2"; # e.g. "gg"
1  ->  5 "\xa3"; # e.g. "gg"
1  ->  5 "\xa4"; # e.g. "gg"
1  ->  5 "\xa5"; # e.g. "gg"
1  ->  5 "\xa6"; # e.g. "gg"
1  ->  5 "\xa7"; # e.g. "gg"
1  ->  5 "\xa8"; # e.g. "gg"
1  ->  5 "\xa9"; # e.g. "gg"
1  ->  5 "\xaa"; # e.g. "gg"
1  ->  5 "\xab"; # e.g. "gg"
1  ->  5 "\xac"; # e.g. "gg"
1  ->  5 "\xad"; # e.g. "gg"
1  ->  5 "\xae"; # e.g. "gg"
1  ->  5 "\xaf"; # e.g. "gg"
1  ->  5 "\xb0"; # e.g. "gg"
1  ->  5 "\xb1"; # e.g. "gg"
1  ->  5 "\xb2"; # e.g. "gg"
1  ->  5 "\xb3"; # e.g. "gg"
1  ->  5 "\xb4"; # e.g. "gg"
1  ->  5 "\xb5"; # e.g. "gg"
1  ->  5 "\xb6"; # e.g. "gg"
1  ->  5 "\xb7"; # e.g. "gg"
1  ->  5 "\xb8"; # e.g. "gg"
1  ->  5 "\xb9"; # e.g. "gg"
1  ->  5 "\xba"; # e.g. "gg"
1  ->  5 "\xbb"; # e.g. "gg"
1  ->  5 "\xbc"; # e.g. "gg"
1  ->  5 "\xbd"; # e.g. "gg"
1  ->  5 "\xbe"; # e.g. "gg"
1  ->  5 "\xbf"; # e.g. "gg"
1  ->  5 "\xc0"; # e.g. "gg"
1  ->  5 "\xc1"; # e.g. "gg"
1  ->  5 "\xc2"; # e.g. "gg"
1  ->  5 "\xc3"; # e.g. "gg"
1  ->  5 "\xc4"; # e.g. "gg"
1  ->  5 "\xc5"; # e.g. "gg"
1  ->  5 "\xc6"; # e.g. "gg"
1  ->  5 "\xc7"; # e.g. "gg"
1  ->  5 "\xc8"; # e.g. "gg"
1  ->  5 "\xc9"; # e.g. "gg"
1  ->  5 "\xca"; # e.g. "gg"
1  ->  5 "\xcb"; # e.g. "gg"
1  ->  5 "\xcc"; # e.g. "gg"
1  ->  5 "\xcd"; # e.g. "gg"
1  ->  5 "\xce"; # e.g. "gg"
1  ->  5 "\xcf"; # e.g. "gg"
1  ->  5 "\xd0"; # e.g. "gg"
1  ->  5 "\xd1"; # e.g. "gg"
1  ->  5 "\xd2"; # e.g. "gg"
1  ->  5 "\xd3"; # e.g. "gg"
1  ->  5 "\xd4"; # e.g. "gg"
1  ->  5 "\xd5"; # e.g. "gg"
1  ->  5 "\xd6"; # e.g. "gg"
1  ->  5 "\xd7"; # e.g. "gg"
1  ->  5 "\xd8"; # e.g. "gg"
1  ->  5 "\xd9"; # e.g. "gg"
1  ->  5 "\xda"; # e.g. "gg"
1  ->  5 "\xdb"; # e.g. "gg"
1  ->  5 "\xdc"; # e.g. "gg"
1  ->  5 "\xdd"; # e.g. "gg"
1  ->  5 "\xde"; # e.g. "gg"
1  ->  5 "\xdf"; # e.g. "gg"
1  ->  5 "\xe0"; # e.g. "gg"
1  ->  5 "\xe1"; # e.g. "gg"
1  ->  5 "\xe2"; # e.g. "gg"
1  ->  5 "\xe3"; # e.g. "gg"
1  ->  5 "\xe4"; # e.g. "gg"
1  ->  5 "\xe5"; # e.g. "gg"
1  ->  5 "\xe6"; # e.g. "gg"
1  ->  5 "\xe7"; # e.g. "gg"
1  ->  5 "\xe8"; # e.g. "gg"
1  ->  5 "\xe9"; # e.g. "gg"
1  ->  5 "\xea"; # e.g. "gg"
1  ->  5 "\xeb"; # e.g. "gg"
1  ->  5 "\xec"; # e.g. "gg"
1  ->  5 "\xed"; # e.g. "gg"
1  ->  5 "\xee"; # e.g. "gg"
1  ->  5 "\xef"; # e.g. "gg"
1  ->  5 "\xf0"; # e.g. "gg"
1  ->  5 "\xf1"; # e.g. "gg"
1  ->  5 "\xf2"; # e.g. "gg"
1  ->  5 "\xf3"; # e.g. "gg"
1  ->  5 "\xf4"; # e.g. "gg"
1  ->  5 "\xf5"; # e.g. "gg"
1  ->  5 "\xf6"; # e.g. "gg"
1  ->  5 "\xf7"; # e.g. "gg"
1  ->  5 "\xf8"; # e.g. "gg"
1  ->  5 "\xf9"; # e.g. "gg"
1  ->  5 "\xfa"; # e.g. "gg"
1  ->  5 "\xfb"; # e.g. "gg"
1  ->  5 "\xfc"; # e.g. "gg"
1  ->  5 "\xfd"; # e.g. "gg"
1  ->  5 "\xfe"; # e.g. "gg"
2  ->  4 "\n"; # e.g. "gg\n\n"
3  ->  5 "\x00"; # e.g. "gg"
3  ->  5 "\x01"; # e.g. "gg"
3  ->  5 "\x02"; # e.g. "gg"
3  ->  5 "\x03"; # e.g. "gg"
3  ->  5 "\x04"; # e.g. "gg"
3  ->  5 "\x05"; # e.g. "gg"
3  ->  5 "\x06"; # e.g. "gg"
3  ->  2 "\x07"; # e.g. "gg\t"
3  ->  5 "\x08"; # e.g. "gg"
3  ->  5 "\t"; # e.g. "gg"
3  ->  6 "\n"; # e.g. "g\n"
3  ->  5 "\v"; # e.g. "gg"
3  ->  5 "\f"; # e.g. "gg"
3  ->  5 "\r"; # e.g. "gg"
3  ->  5 "\x0e"; # e.g. "gg"
3  ->  5 "\x0f"; # e.g. "gg"
3  ->  5 "\x10"; # e.g. "gg"
3  ->  5 "\x11"; # e.g. "gg"
3  ->  5 "\x12"; # e.g. "gg"
3  ->  5 "\x13"; # e.g. "gg"
3  ->  5 "\x14"; # e.g. "gg"
3  ->  5 "\x15"; # e.g. "gg"
3  ->  5 "\x16"; # e.g. "gg"
3  ->  5 "\x17"; # e.g. "gg"
3  ->  5 "\x18"; # e.g. "gg"
3  ->  5 "\x19"; # e.g. "gg"
3  ->  5 "\x1a"; # e.g. "gg"
3  ->  5 "\x1b"; # e.g. "gg"
3  ->  5 "\x1c"; # e.g. "gg"
3  ->  5 "\x1d"; # e.g. "gg"
3  ->  5 "\x1e"; # e.g. "gg"
3  ->  5 "\x1f"; # e.g. "gg"
3  ->  5 " "; # e.g. "gg"
3  ->  5 "!"; # e.g. "gg"
3  ->  5 "\""; # e.g. "gg"
3  ->  5 "#"; # e.g. "gg"
3  ->  5 "$"; # e.g. "gg"
3  ->  5 "%"; # e.g. "gg"
3  ->  5 "&"; # e.g. "gg"
3  ->  5 "'"; # e.g. "gg"
3  ->  5 "("; # e.g. "gg"
3  ->  5 ")"; # e.g. "gg"
3  ->  5 "*"; # e.g. "gg"
3  ->  5 "+"; # e.g. "gg"
3  ->  5 ","; # e.g. "gg"
3  ->  5 "."; # e.g. "gg"
3  ->  5 "/"; # e.g. "gg"
3  ->  5 "0"; # e.g. "gg"
3  ->  5 "1"; # e.g. "gg"
3  ->  5 "2"; # e.g. "gg"
3  ->  5 "3"; # e.g. "gg"
3  ->  5 "4"; # e.g. "gg"
3  ->  5 "5"; # e.g. "gg"
3  ->  5 "6"; # e.g. "gg"
3  ->  5 "7"; # e.g. "gg"
3  ->  5 "8"; # e.g. "gg"
3  ->  5 "9"; # e.g. "gg"
3  ->  5 ":"; # e.g. "gg"
3  ->  5 ";"; # e.g. "gg"
3  ->  5 "<"; # e.g. "gg"
3  ->  5 "="; # e.g. "gg"
3  ->  5 ">"; # e.g. "gg"
3  ->  5 "?"; # e.g. "gg"
3  ->  5 "@"; # e.g. "gg"
3  ->  5 "A"; # e.g. "gg"
3  ->  5 "B"; # e.g. "gg"
3  ->  5 "C"; # e.g. "gg"
3  ->  5 "D"; # e.g. "gg"
3  ->  5 "E"; # e.g. "gg"
3  ->  5 "F"; # e.g. "gg"
3  ->  5 "G"; # e.g. "gg"
3  ->  5 "H"; # e.g. "gg"
3  ->  5 "I"; # e.g. "gg"
3  ->  5 "J"; # e.g. "gg"
3  ->  5 "K"; # e.g. "gg"
3  ->  5 "L"; # e.g. "gg"
3  ->  5 "M"; # e.g. "gg"
3  ->  5 "N"; # e.g. "gg"
3  ->  5 "O"; # e.g. "gg"
3  ->  5 "P"; # e.g. "gg"
3  ->  5 "Q"; # e.g. "gg"
3  ->  5 "R"; # e.g. "gg"
3  ->  5 "S"; # e.g. "gg"
3  ->  5 "T"; # e.g. "gg"
3  ->  5 "U"; # e.g. "gg"
3  ->  5 "V"; # e.g. "gg"
3  ->  5 "W"; # e.g. "gg"
3  ->  5 "X"; # e.g. "gg"
3  ->  5 "Y"; # e.g. "gg"
3  ->  5 "Z"; # e.g. "gg"
3  ->  5 "["; # e.g. "gg"
3  ->  5 "\\"; # e.g. "gg"
3  ->  5 "]"; # e.g. "gg"
3  ->  5 "_"; # e.g. "gg"
3  ->  5 "`"; # e.g. "gg"
3  ->  5 "g"; # e.g. "gg"
3  ->  5 "h"; # e.g. "gg"
3  ->  5 "i"; # e.g. "gg"
3  ->  5 "j"; # e.g. "gg"
3  ->  5 "k"; # e.g. "gg"
3  ->  5 "l"; # e.g. "gg"
3  ->  5 "m"; # e.g. "gg"
3  ->  5 "n"; # e.g. "gg"
3  ->  5 "o"; # e.g. "gg"
3  ->  5 "p"; # e.g. "gg"
3  ->  5 "q"; # e.g. "gg"
3  ->  5 "r"; # e.g. "gg"
3  ->  5 "s"; # e.g. "gg"
3  ->  5 "t"; # e.g. "gg"
3  ->  5 "u"; # e.g. "gg"
3  ->  5 "v"; # e.g. "gg"
3  ->  5 "w"; # e.g. "gg"
3  ->  5 "x"; # e.g. "gg"
3  ->  5 "y"; # e.g. "gg"
3  ->  5 "z"; # e.g. "gg"
3  ->  5 "{"; # e.g. "gg"
3  ->  5 "|"; # e.g. "gg"
3  ->  5 "}"; # e.g. "gg"
3  ->  5 "~"; # e.g. "gg"
3  ->  5 "\x7f"; # e.g. "gg"
3  ->  5 "\x80"; # e.g. "gg"
3  ->  5 "\x81"; # e.g. "gg"
3  ->  5 "\x82"; # e.g. "gg"
3  ->  5 "\x83"; # e.g. "gg"
3  ->  5 "\x84"; # e.g. "gg"
3  ->  5 "\x85"; # e.g. "gg"
3  ->  5 "\x86"; # e.g. "gg"
3  ->  5 "\x87"; # e.g. "gg"
3  ->  5 "\x88"; # e.g. "gg"
3  ->  5 "\x89"; # e.g. "gg"
3  ->  5 "\x8a"; # e.g. "gg"
3  ->  5 "\x8b"; # e.g. "gg"
3  ->  5 "\x8c"; # e.g. "gg"
3  ->  5 "\x8d"; # e.g. "gg"
3  ->  5 "\x8e"; # e.g. "gg"
3  ->  5 "\x8f"; # e.g. "gg"
3  ->  5 "\x90"; # e.g. "gg"
3  ->  5 "\x91"; # e.g. "gg"
3  ->  5 "\x92"; # e.g. "gg"
3  ->  5 "\x93"; # e.g. "gg"
3  ->  5 "\x94"; # e.g. "gg"
3  ->  5 "\x95"; # e.g. "gg"
3  ->  5 "\x96"; # e.g. "gg"
3  ->  5 "\x97"; # e.g. "gg"
3  ->  5 "\x98"; # e.g. "gg"
3  ->  5 "\x99"; # e.g. "gg"
3  ->  5 "\x9a"; # e.g. "gg"
3  ->  5 "\x9b"; # e.g. "gg"
3  ->  5 "\x9c"; # e.g. "gg"
3  ->  5 "\x9d"; # e.g. "gg"
3  ->  5 "\x9e"; # e.g. "gg"
3  ->  5 "\x9f"; # e.g. "gg"
3  ->  5 "\xa0"; # e.g. "gg"
3  ->  5 "\xa1"; # e.g. "gg"
3  ->  5 "\xa2"; # e.g. "gg"
3  ->  5 "\xa3"; # e.g. "gg"
3  ->  5 "\xa4"; # e.g. "gg"
3  ->  5 "\xa5"; # e.g. "gg"
3  ->  5 "\xa6"; # e.g. "gg"
3  ->  5 "\xa7"; # e.g. "gg"
3  ->  5 "\xa8"; # e.g. "gg"
3  ->  5 "\xa9"; # e.g. "gg"
3  ->  5 "\xaa"; # e.g. "gg"
3  ->  5 "\xab"; # e.g. "gg"
3  ->  5 "\xac"; # e.g. "gg"
3  ->  5 "\xad"; # e.g. "gg"
3  ->  5 "\xae"; # e.g. "gg"
3  ->  5 "\xaf"; # e.g. "gg"
3  ->  5 "\xb0"; # e.g. "gg"
3  ->  5 "\xb1"; # e.g. "gg"
3  ->  5 "\xb2"; # e.g. "gg"
3  ->  5 "\xb3"; # e.g. "gg"
3  ->  5 "\xb4"; # e.g. "gg"
3  ->  5 "\xb5"; # e.g. "gg"
3  ->  5 "\xb6"; # e.g. "gg"
3  ->  5 "\xb7"; # e.g. "gg"
3  ->  5 "\xb8"; # e.g. "gg"
3  ->  5 "\xb9"; # e.g. "gg"
3  ->  5 "\xba"; # e.g. "gg"
3  ->  5 "\xbb"; # e.g. "gg"
3  ->  5 "\xbc"; # e.g. "gg"
3  ->  5 "\xbd"; # e.g. "gg"
3  ->  5 "\xbe"; # e.g. "gg"
3  ->  5 "\xbf"; # e.g. "gg"
3  ->  5 "\xc0"; # e.g. "gg"
3  ->  5 "\xc1"; # e.g. "gg"
3  ->  5 "\xc2"; # e.g. "gg"
3  ->  5 "\xc3"; # e.g. "gg"
3  ->  5 "\xc4"; # e.g. "gg"
3  ->  5 "\xc5"; # e.g. "gg"
3  ->  5 "\xc6"; # e.g. "gg"
3  ->  5 "\xc7"; # e.g. "gg"
3  ->  5 "\xc8"; # e.g. "gg"
3  ->  5 "\xc9"; # e.g. "gg"
3  ->  5 "\xca"; # e.g. "gg"
3  ->  5 "\xcb"; # e.g. "gg"
3  ->  5 "\xcc"; # e.g. "gg"
3  ->  5 "\xcd"; # e.g. "gg"
3  ->  5 "\xce"; # e.g. "gg"
3  ->  5 "\xcf"; # e.g. "gg"
3  ->  5 "\xd0"; # e.g. "gg"
3  ->  5 "\xd1"; # e.g. "gg"
3  ->  5 "\xd2"; # e.g. "gg"
3  ->  5 "\xd3"; # e.g. "gg"
3  ->  5 "\xd4"; # e.g. "gg"
3  ->  5 "\xd5"; # e.g. "gg"
3  ->  5 "\xd6"; # e.g. "gg"
3  ->  5 "\xd7"; # e.g. "gg"
3  ->  5 "\xd8"; # e.g. "gg"
3  ->  5 "\xd9"; # e.g. "gg"
3  ->  5 "\xda"; # e.g. "gg"
3  ->  5 "\xdb"; # e.g. "gg"
3  ->  5 "\xdc"; # e.g. "gg"
3  ->  5 "\xdd"; # e.g. "gg"
3  ->  5 "\xde"; # e.g. "gg"
3  ->  5 "\xdf"; # e.g. "gg"
3  ->  5 "\xe0"; # e.g. "gg"
3  ->  5 "\xe1"; # e.g. "gg"
3  ->  5 "\xe2"; # e.g. "gg"
3  ->  5 "\xe3"; # e.g. "gg"
3  ->  5 "\xe4"; # e.g. "gg"
3  ->  5 "\xe5"; # e.g. "gg"
3  ->  5 "\xe6"; # e.g. "gg"
3  ->  5 "\xe7"; # e.g. "gg"
3  ->  5 "\xe8"; # e.g. "gg"
3  ->  5 "\xe9"; # e.g. "gg"
3  ->  5 "\xea"; # e.g. "gg"
3  ->  5 "\xeb"; # e.g. "gg"
3  ->  5 "\xec"; # e.g. "gg"
3  ->  5 "\xed"; # e.g. "gg"
3  ->  5 "\xee"; # e.g. "gg"
3  ->  5 "\xef"; # e.g. "gg"
3  ->  5 "\xf0"; # e.g. "gg"
3  ->  5 "\xf1"; # e.g. "gg"
3  ->  5 "\xf2"; # e.g. "gg"
3  ->  5 "\xf3"; # e.g. "gg"
3  ->  5 "\xf4"; # e.g. "gg"
3  ->  5 "\xf5"; # e.g. "gg"
3  ->  5 "\xf6"; # e.g. "gg"
3  ->  5 "\xf7"; # e.g. "gg"
3  ->  5 "\xf8"; # e.g. "gg"
3  ->  5 "\xf9"; # e.g. "gg"
3  ->  5 "\xfa"; # e.g. "gg"
3  ->  5 "\xfb"; # e.g. "gg"
3  ->  5 "\xfc"; # e.g. "gg"
3  ->  5 "\xfd"; # e.g. "gg"
3  ->  5 "\xfe"; # e.g. "gg"
5  ->  2 "\x00"; # e.g. "gg\t"
5  ->  2 "\x01"; # e.g. "gg\t"
5  ->  2 "\x02"; # e.g. "gg\t"
5  ->  2 "\x03"; # e.g. "gg\t"
5  ->  2 "\x04"; # e.g. "gg\t"
5  ->  2 "\x05"; # e.g. "gg\t"
5  ->  2 "\x06"; # e.g. "gg\t"
5  ->  2 "\x07"; # e.g. "gg\t"
5  ->  2 "\x08"; # e.g. "gg\t"
5  ->  2 "\t"; # e.g. "gg\t"
5  ->  7 "\n"; # e.g. "gg\n"
5  ->  2 "\v"; # e.g. "gg\t"
5  ->  2 "\f"; # e.g. "gg\t"
5  ->  2 "\r"; # e.g. "gg\t"
5  ->  2 "\x0e"; # e.g. "gg\t"
5  ->  2 "\x0f"; # e.g. "gg\t"
5  ->  2 "\x10"; # e.g. "gg\t"
5  ->  2 "\x11"; # e.g. "gg\t"
5  ->  2 "\x12"; # e.g. "gg\t"
5  ->  2 "\x13"; # e.g. "gg\t"
5  ->  2 "\x14"; # e.g. "gg\t"
5  ->  2 "\x15"; # e.g. "gg\t"
5  ->  2 "\x16"; # e.g. "gg\t"
5  ->  2 "\x17"; # e.g. "gg\t"
5  ->  2 "\x18"; # e.g. "gg\t"
5  ->  2 "\x19"; # e.g. "gg\t"
5  ->  2 "\x1a"; # e.g. "gg\t"
5  ->  2 "\x1b"; # e.g. "gg\t"
5  ->  2 "\x1c"; # e.g. "gg\t"
5  ->  2 "\x1d"; # e.g. "gg\t"
5  ->  2 "\x1e"; # e.g. "gg\t"
5  ->  2 "\x1f"; # e.g. "gg\t"
5  ->  2 "\x7f"; # e.g. "gg\t"
6  ->  2 "\x00"; # e.g. "gg\t"
6  ->  2 "\x01"; # e.g. "gg\t"
6  ->  2 "\x02"; # e.g. "gg\t"
6  ->  2 "\x03"; # e.g. "gg\t"
6  ->  2 "\x04"; # e.g. "gg\t"
6  ->  2 "\x05"; # e.g. "gg\t"
6  ->  2 "\x06"; # e.g. "gg\t"
6  ->  2 "\x07"; # e.g. "gg\t"
6  ->  2 "\x08"; # e.g. "gg\t"
6  ->  2 "\t"; # e.g. "gg\t"
6  ->  7 "\n"; # e.g. "gg\n"
6  ->  2 "\v"; # e.g. "gg\t"
6  ->  2 "\f"; # e.g. "gg\t"
6  ->  2 "\r"; # e.g. "gg\t"
6  ->  2 "\x0e"; # e.g. "gg\t"
6  ->  2 "\x0f"; # e.g. "gg\t"
6  ->  2 "\x10"; # e.g. "gg\t"
6  ->  2 "\x11"; # e.g. "gg\t"
6  ->  2 "\x12"; # e.g. "gg\t"
6  ->  2 "\x13"; # e.g. "gg\t"
6  ->  2 "\x14"; # e.g. "gg\t"
6  ->  2 "\x15"; # e.g. "gg\t"
6  ->  2 "\x16"; # e.g. "gg\t"
6  ->  2 "\x17"; # e.g. "gg\t"
6  ->  2 "\x18"; # e.g. "gg\t"
6  ->  2 "\x19"; # e.g. "gg\t"
6  ->  2 "\x1a"; # e.g. "gg\t"
6  ->  2 "\x1b"; # e.g. "gg\t"
6  ->  2 "\x1c"; # e.g. "gg\t"
6  ->  2 "\x1d"; # e.g. "gg\t"
6  ->  2 "\x1e"; # e.g. "gg\t"
6  ->  2 "\x1f"; # e.g. "gg\t"
6  ->  2 "\x7f"; # e.g. "gg\t"
7  ->  4 "\n"; # e.g. "gg\n\n"

start: 0;
end:   3, 4, 6, 7;
